TRENDZO — AI EMPLOYEE ARCHITECTURE (v2)
Substrate-First Build Blueprint
Date: 2026-04-22  |  Version: 2.0  |  Status: LIVE — supersedes v1.0 (2026-04-14)

WHY THIS DOCUMENT EXISTS
v1 described an end state, not a current state. v2 describes both.
The cost of that omission compounded. Six tables v1 named don't exist. Workflows marked BUILT aren't. "Both UIs parity" was a rule imposed on two different architectures instead of a consequence of shared foundation. Cursor prompts referencing v1 inherited the drift. The system built against it is now 60% real, 40% scaffolding painted to look done. This document fixes that.
v2 does three things v1 didn't:

Names Trendzo's substrate layers explicitly — the invisible foundation that must exist before any AI Employee workflow can be a 48-hour feature.
Labels every workflow with honest STATUS, DISTANCE, and DEPENDENCIES, grounded in audit evidence.
Re-sequences the build order by substrate completion, then by feature unlock — so every phase ships faster than the one before it.


THE SIX SUBSTRATE LAYERS
Trendzo's substrate is the Creator Operations Graph. It is composed of six layers. Each layer must be BUILT before the layer above it can ship honest features.
Layer 0 — Data Ingestion
What it is: TikTok videos in, 58-feature vectors out.
Status: BUILT.
Evidence: Bulk feature extraction pipeline operational. 6,718 videos processed into training cohort. Apify + internal scrapers feeding scraped_videos, video_files, prediction_runs. Feature extraction flows into creator_video_history and run_component_results.
Known drift signals (would unBUILD this layer): scraped_videos CREATE TABLE lives in scripts/, not migrations/ — schema source of truth is ambiguous. Flag for cleanup, not blocking.
Layer 1 — The Scoring Engine
What it is: 58 features in, DPS + VPS out with component-level explainability.
Status: BUILT.
Evidence: runPredictionPipeline.ts operational. XGBoost inference producing VPS with Spearman ρ = 0.61. DPS v2.1.0 with 8-signal architecture locked. Writes to prediction_runs + run_component_results.
Known gaps: Migration named 20260329_dps_v2_incomplete_columns.sql — the name itself signals unfinished work. Needs audit. Not blocking downstream work but should be resolved before Layer 4 depends heavily on DPS v2.
Layer 2 — The Event Log
What it is: Every meaningful action in the system — brief delivered, score computed, creator acknowledged, prediction beat, memory promoted — written as an immutable event to a single canonical table. Queryable for any "how is this performing over time" question, forever.
Status: PARTIAL — this is the most dangerous gap in the stack.
An event log that's only sometimes written to is worse than no event log at all, because it produces confidently-wrong analytics.
Evidence: platform_events table exists (20260319_platform_events.sql, extended 20260418). emitEvent() helper exists (src/lib/events/emit.ts). Emission is inconsistent — two Clay write adapters emit agent.proposal events, the two brief routes locked in Fix 2 now emit audit events, but most mutation paths across the codebase don't emit anything. No enforcement that state mutations must emit. No event taxonomy document. No read-side consumer treating this as canonical.
Why this layer being PARTIAL is dangerous: Every AI Employee feature that asks "what happened?" quietly assumes this layer is complete. Weekly performance reports. Accuracy trend analysis. Coaching feedback grounded in history. Chase sequences. Prediction improvement tracking. Those features will either be built on one-off queries against domain tables (fragile, expensive to change) or built on a half-populated event log (wrong numbers, silent failures). Neither is acceptable.
What "BUILT" would look like (spec):

Every mutation in every API route under /api/agency/*, /api/brief-*, /api/creator/* emits a platform_events row within the same database transaction as the mutation. Enforced by code review + a lint rule that flags mutation routes without emission.
A docs/event-taxonomy.md file lists every valid event_type with its required payload shape. No new event types outside this registry.
At least one read-side consumer (the nightly memory consolidation agent is a natural candidate) treats platform_events as source of truth for "what happened" — not a supplementary log.

Layer 3 — Memory
What it is: Curated, tiered, decayable state about creators and agencies — distinct from the Event Log. The Event Log is immutable history; Memory is promoted, contradicted, consolidated, and expired facts. A different read pattern, a different write rule, a different consumer set.
Status: PARTIAL (leaning BUILT).
Evidence: memory_extractions table exists with hot/warm/cold tiers, confidence scores, reference counts, and supersession logic (20260407_memory_system.sql). Nightly consolidation agent (/api/cron/consolidate-memory) uses Gemini 2.5 Flash to merge, resolve contradictions, and demote stale facts — real LLM agent, ~500 lines, scheduled and running. assembleContext() injects hot memory into every agency-chat LLM call. Clay's 24-hour conversation persistence is a separate but adjacent primitive (agency_conversations, 20260401).
Why it's PARTIAL not BUILT: Per-creator warm memory has a read path (fetchWarmMemory() filters by creator_id) but no confirmed write path for creator-tagged facts. Conversation extraction might populate it via consolidation, but that pipeline isn't traced end-to-end yet. Also: no operator-facing memory surface outside Clay's MemoryFactCard. "What does Trendzo know about this creator?" has no direct answer page.
What "BUILT" would look like (spec):

Conversation extraction → memory_extractions write path documented and traced. A fact surfaced in a conversation about creator X ends up tagged to creator X's warm memory within 24 hours.
Operator-facing memory surface at /agency/memory showing both agency-level hot facts and per-creator warm facts. Read/edit/delete available.
Memory write path separate from Event Log write path. Events record that something happened; memory records what the system now believes is true.

Layer 4 — The Creator Operations Graph
What it is: The single source of truth for every creator — identity, fingerprint, agency relationship, brief history, performance trajectory, memory facts, onboarding state — unified and synchronized such that every surface reads consistent data regardless of which table it queries.
Status: NOT BUILT as a unified graph. Components exist, unified substrate does not.
Evidence: Three parallel creator tables coexist with zero synchronization: creators (legacy admin roster, consumed by MCP server + admin dashboard), creator_profiles (prediction-side TikTok baseline, no auth link, consumed by prediction routes), onboarding_profiles (canonical per the merge migration comment, consumed by agency-chat + Dashboard). A creator added via one flow does not appear in the other flows.
Why this layer being NOT BUILT is the reason features fracture: The Creator Operations Graph was named as Trendzo's substrate in the original vision (see GitHub_repos doc §2b). It's the Rippling Employee Graph equivalent. Every AI Employee assumes they're operating on the same creator record. They aren't.
What "BUILT" would look like (spec — decision made, not pending):
Canonical table: onboarding_profiles. Chosen because (a) it's already consumed by the surfaces new work is happening on — agency-chat and Dashboard, (b) it already has the agency_id column Fix 1 depended on, (c) the merge migration comment explicitly names it canonical, (d) creators is scoped to legacy admin/billing concerns and creator_profiles is scoped to prediction — both are narrower than the unified identity the Graph requires.
Consolidation rules:

onboarding_profiles.id becomes the canonical creator identifier. All new FKs point at it. Fix 1's content_briefs.agency_id pattern generalizes: every creator-scoped table gets a nullable creator_id FK at minimum.
creators is frozen. Migration adds a -- DEPRECATED: read-only. Source of truth is onboarding_profiles. banner comment. No new writes. Admin dashboards continue reading it until migrated; all new admin work reads from onboarding_profiles joined to profiles.
creator_profiles freezes in its current state during Phase A3 — read-only, stale but functional. The rebuild job that converts it to a live materialized projection of onboarding_profiles + prediction data ships in Phase F (Managed Autonomy), because creating a cron entry before the autonomy substrate is properly built is the exact v1 failure mode. Write access is revoked from all routes except that future rebuild job.
calibration_profiles remains as a preserved backup per its existing migration comment. No action needed.

Verification (Phase A3 exit criteria): Six greps must all return zero matches in src/ (excluding the future rebuild job path):
grep -r ".from('creators').insert(" src/
grep -r ".from('creators').update(" src/
grep -r ".from('creators').upsert(" src/
grep -r ".from('creator_profiles').insert(" src/
grep -r ".from('creator_profiles').update(" src/
grep -r ".from('creator_profiles').upsert(" src/
A grep for .from('onboarding_profiles') returns the writes that replaced them. Three operations × two tables = six total patterns. If any pattern returns a hit that isn't the rebuild job, Phase A3 is not done.
Layer 5 — Capability Surfaces
What it is: The attachment points where visible features plug into substrate. A consistent pattern for adding a new capability such that it appears in Clay (as a tool call or component) AND the Dashboard (as a button/view/card) from a single registration, not two separate builds.
Status: PARTIAL — asymmetric.
Evidence: Clay side is real. Tool registry exists (src/lib/agent/tool-registry.ts). Component registry exists (src/lib/clay/intelligent-clay-registry.ts). Adapter pattern emits propose/real tool pairs (src/lib/agent/handler-adapters.ts). Dashboard side has no equivalent — adding a new Dashboard capability means editing DashboardClient.tsx by hand. "Both UIs parity" is enforced by discipline, not architecture.
Why this layer being PARTIAL is the reason parity keeps breaking: The "both UIs" rule only works if both UIs consume from the same registration. Right now they consume from two different codebases that happen to point at overlapping API routes. Every new capability requires building the same thing twice and remembering to keep them in sync. The audit shows this already breaking: send_invite is stubbed on the Clay side and has no Dashboard equivalent; generate_batch_briefs is handler-only with no Clay tool pair.
What "BUILT" would look like (spec):

A single defineCapability() function that accepts one declaration and produces: (a) a Clay tool call (propose/real pair if it's a write action), (b) a Clay component for rendering, (c) a Dashboard UI element matched to the declared category (button / card / table / kanban slot), (d) the API route handler, (e) event emissions on mutations.
src/capabilities/ directory holding one file per capability. Registry auto-discovers them on startup.
Adding a new capability is: write one file, app picks it up, both UIs see it. Removing is: delete one file, both UIs lose it.

Layer 6 — Managed Autonomy
What it is: The infrastructure that lets AI Employees actually run without human triggers. Scheduled cron jobs. Long-running agent sessions. Self-scheduled actions. The difference between "we have code that could act autonomously" and "the system is acting autonomously right now."
Status: PARTIAL.
Evidence: vercel.json schedules 3 cron jobs (recency-decay weekly, freedom-agent weekly, feedback-collector every 6h). Meanwhile, cron routes exist but are NOT scheduled for: autodream (overnight brief generation), cultural-scan (trend detection), classify-events, training-pipeline, consolidate-memory (despite being the Memory layer's consolidation agent). The nightly Memory Keeper logic is written and autonomous-capable. The trend scout, overnight brief writer, and autonomous account manager behaviors are code that happens to exist but doesn't run on its own.
Why this layer is separate from Capability Surfaces: A cron-triggered execution path is a different runtime than a user-triggered API call. Different failure modes (silent vs visible). Different observability needs (operator won't see a cron failure unless we surface it). Different permission model (system identity vs user identity). Different rollback semantics. Combining them would let unscheduled "autonomous" features hide behind capability registrations and never get the autonomy spec.
What "BUILT" would look like (spec):

Every claimed autonomous behavior has either a vercel.json cron entry or a documented live-session trigger. Zero routes labeled autonomous that only run when a human clicks.
A docs/autonomy.md file listing every scheduled job with its cadence, observability hook, and failure-alerting path.
Cron failures surface in the operator-facing alert inbox, not just in server logs.
Phase F deliverable: the creator_profiles rebuild job deferred from Phase A3 lands here, with its own cron entry and alerting.


SCHEMA SOURCE OF TRUTH
The only valid reference for database shape is supabase/migrations/*.sql.
Not this document. Not v1. Not code comments. Not the MCP server's table references. Not READMEs. If a migration doesn't declare it, it doesn't exist — and any code assuming it does exist is a bug, not a feature.
Known current drifts (as of 2026-04-21 audit) that every build prompt must respect until resolved:
What a prompt might sayRealityinvitations tableReal table is agency_invites. invitations does not exist.invitations.nudge_count / last_nudge_at / nudge_stageNone of these columns exist on agency_invites.trend_creator_matchesDoes not exist. Workflow TS-2 has no schema.creator_alertsDoes not exist. Workflow TS-4 has no schema.creator_feedbackDoes not exist. Workflow PA-4 has no schema.performance_reportsDoes not exist. Workflow PA-3 has no schema.chase_logDoes not exist. Workflow PM-5 has no schema.scheduled_postsDoes not exist. (There is content_briefs.scheduled_publish_at + tiktok_drafts but no scheduled_posts table.)agency_invitations (plural)Real table is agency_invites (singular). Plural queries return empty silently.content_briefs.nudge_sent_atReal column is last_nudged_at.content_briefs.vps_prediction vs predicted_vpsBOTH EXIST — active dual-column drift. No single source of truth yet. Phase A1 resolves.content_briefs.status vs completion_statusBOTH EXIST — status is legacy Quick Win, completion_status is AM lifecycle. Phase A1 resolves.agencies.statusColumn does not exist. Real column is is_active. Two cron routes query the nonexistent column and get null back silently.
Every build prompt that touches schema must cross-check against the live migration directory before referencing a column or table name. Claude Code's Step 1 diagnostic must confirm schema existence before writing any migration or query.

THE LIFECYCLE (UNCHANGED FROM v1)
ONBOARD → PROFILE → BRIEF → DELIVER → PRODUCE → PUBLISH → MEASURE → COACH → REPEAT
Every AI Employee owns a segment of this lifecycle. Every segment must work in both Intelligent Clay (/agency) and Traditional Dashboard (/agency/dashboard) from a single capability registration (once Layer 5 is BUILT).

STATUS / DISTANCE / DEPENDENCIES FORMAT
Every AI Employee workflow in the sections that follow is labeled with three fields. This is what lets this document function as a buildable roadmap instead of a wishlist.
STATUS is one of four values:

BUILT — fully working in both UIs, wired to substrate, verified against audit.
PARTIAL — some of the pieces exist (schema, API, UI, tool call, auto-trigger) but not all of them. PARTIAL is specified as a list of what exists and what doesn't.
NOT BUILT — no real implementation. Schema may exist but nothing uses it, or schema doesn't exist and neither does anything else.
DEPRECATED — was in v1, removed from v2 because it depends on substrate that shouldn't be built, or it's a feature we no longer intend to ship.

DISTANCE is the honest estimate of time from current status to BUILT, assuming all dependencies are already BUILT. Expressed in founder-hours, not calendar time:

Hours (2-8 hours) — wiring and glue. No new substrate needed.
Days (1-3 days) — meaningful feature work. Substrate ready, logic to build.
Weeks (1-3 weeks) — significant work. Likely triggers follow-on work.
Blocked — cannot be estimated until dependencies resolve.

DEPENDENCIES is the explicit list of substrate layers and other workflows that must be BUILT before this workflow can be a 48-hour feature. If any dependency is PARTIAL or NOT BUILT, this workflow inherits BLOCKED status regardless of DISTANCE.

BUILD ORDER — RE-SEQUENCED FROM v1
Phase A is invisible work, and that's the point.
v1 sequenced by AI Employee (Account Manager → Onboarding → Trend Scout → Performance → Project Manager). That order is wrong because it front-loads workflows whose substrate dependencies are broken. Every AM workflow touching performance data would inherit the Event Log gap. Every Onboarding workflow writing a creator would inherit the three-table fracture. The features would look built and produce wrong numbers.
v2 sequences by substrate completion, then by feature unlock. Phases A through F build substrate. Phases G through I build features. The trade is apparent speed for real speed — Phase A ships no visible progress, but every phase after it ships faster than it would have under v1 sequencing.
If you read this doc on a hard day and feel the urge to skip Phase A to get to something visible, that urge is the problem v1 was caused by. Resist it.
PHASE A — Close the substrate holes Fix 1 and Fix 2 exposed
A1. Resolve content_briefs dual-column drift. Pick authoritative columns for vps_prediction vs predicted_vps and status vs completion_status. Deprecate the losers with comment banners. Migrate consumers.
A2. Event Log hardening (Layer 2 → BUILT). Event taxonomy document, universal emission on mutations in agency/brief/creator routes, one read-side consumer (likely the memory consolidation agent) treating events as canonical.
A3. Creator Operations Graph consolidation (Layer 4 → BUILT). Execute the spec in Layer 4 above: freeze creators, freeze creator_profiles in a stale-but-readable state (the rebuild job lands in Phase F), point new FKs at onboarding_profiles.id. Exit criteria: the six verification greps all return zero non-rebuild-job matches.
PHASE B — Complete the Account Manager

Accepted technical debt: Phase B will be built on the current asymmetric capability pattern — hand-built Dashboard UI alongside Clay tool calls. These workflows will be refactored in Phase E when Capability Surfaces unify. The cost is accepted because parity from Phase E onward is automatic instead of policed.

Five workflows (AM-1 through AM-5). Most are wiring on top of now-solid substrate. Shortest distance to "one AI Employee fully built in both UIs."
PHASE C — Complete the Onboarding Specialist
Four workflows (OB-1 through OB-4). Requires Phase A3's Creator Operations Graph consolidation. Cannot start until A3 is done.
PHASE D — Memory Layer completion (Layer 3 → BUILT)

Accepted technical debt: Phase D's operator-facing surface at /agency/memory will be hand-built on the current asymmetric capability pattern and refactored in Phase E. The cost is accepted because parity from Phase E onward is automatic instead of policed.

Per-creator memory write path traced end-to-end. Operator-facing memory surface at /agency/memory unified for agency + per-creator view.
PHASE E — Capability Surfaces unification (Layer 5 → BUILT)
Replace Dashboard's hand-built UI with a registration-driven shell consuming the same capability declarations Clay consumes. After this, "both UIs parity" is automatic, not policed. Phases B and D workflows get refactored to the new pattern here.
PHASE F — Managed Autonomy backbone (Layer 6 → BUILT)
Schedule every cron that exists but isn't scheduled. Wire cron-failure alerts to operator inbox. Document what's autonomous and what isn't. Phase A3's deferred creator_profiles rebuild job ships as a cron here.
PHASE G — Trend Scout
Four workflows (TS-1 through TS-4). Requires Layers 2, 4, 5, and 6 BUILT. This is the feature that makes creators log in daily.
PHASE H — Performance Analyst
Five workflows (PA-1 through PA-5). Requires Phase B's accumulated brief performance data + Layer 2 events as source of truth.
PHASE I — Project Manager / Coordinator
Five workflows (PM-1 through PM-5). Requires Phases B, C, and G complete.

DOCUMENT CONVENTIONS FOR ALL SECTIONS BELOW
Each AI Employee section contains:

Role summary (what this employee owns in the lifecycle)
What it replaces (the human work being displaced, with approximate hours saved)
Per-workflow detail table with: Trigger, Action, Clay UX, Dashboard UX, DB writes, Failure handling, STATUS, DISTANCE, DEPENDENCIES
Build gaps summary (matrix of Schema / API / Dashboard / Clay / Auto-trigger)
Tool Call Registry contribution

END OF OPENING SECTION.


AI EMPLOYEE 1 — ACCOUNT MANAGER
Role summary
The Account Manager owns the middle of the lifecycle: DELIVER → PRODUCE → PUBLISH. Once an operator approves a brief, the Account Manager is responsible for getting it into the creator's hands, confirming they received it, nudging them when they go silent, tracking the brief through production, and closing the loop when the video ships.
What it replaces
The human at an agency who sends briefs to creators, follows up when they don't respond, checks in on production status, and confirms publication. At a 10-creator agency with ~14 active briefs on average, this is roughly 6-10 hours/week of DM-chasing, spreadsheet-updating, and calendar-watching. At scale (40+ creators) it becomes a full-time role.
Substrate assumption
This section is written assuming Phase A is complete. That means:

Layer 0 (Data Ingestion): BUILT (was BUILT pre-Phase A)
Layer 1 (Scoring Engine): BUILT (was BUILT pre-Phase A)
Layer 2 (Event Log): BUILT via Phase A2. Every mutation route emits to platform_events. Event taxonomy documented.
Layer 3 (Memory): PARTIAL — Phase D completes this. AM workflows do not depend on Phase D.
Layer 4 (Creator Operations Graph): BUILT via Phase A3. onboarding_profiles is canonical. creators and creator_profiles are frozen.
Layer 5 (Capability Surfaces): PARTIAL — Phase E completes this. Per the accepted technical debt callout on Phase B, AM workflows are built on the current asymmetric pattern and refactored in Phase E.
Layer 6 (Managed Autonomy): PARTIAL — Phase F completes this. AM-3's auto-trigger requires Phase F; the manual nudge path does not.

Per-workflow detail
AM-1 — Brief Delivery
FieldDetailTriggerOperator approves a brief (in Clay or Dashboard).ActionSystem emails the brief to the creator via SMTP. Writes delivery timestamp and status to content_briefs. Emits brief.delivered event to platform_events.Clay UX"Approve Jake's brief" → Clay calls the approve-brief handler → "Done. Brief delivered to jake@email.com at 2:14 PM."Dashboard UXApprove button on the brief card → green "Delivered" badge appears with timestamp.DB writescontent_briefs.delivery_status → delivered, content_briefs.delivered_at → NOW(), content_briefs.completion_status → delivered. platform_events row with event_type = 'brief.delivered'.Failure handlingIf SMTP fails: delivery_status → failed, error logged, operator alerted. Retry available in both UIs. Delivery is idempotent — re-sending does not duplicate the content_briefs row; it updates timestamp and increments a delivery attempt counter.STATUSBUILT. Evidence: audit §3.1 marks this COMPLETE. src/lib/email/send-brief.ts:122 handles SMTP. src/app/api/agency/brief-review/route.ts:208 fires the approve path. No dedicated Clay tool — it rides on the approve_brief action. Post-Phase A2, platform_events emission is part of the approve path.DISTANCEAlready BUILT. Zero additional work.DEPENDENCIESLayer 2 (Event Log) for audit emission. Otherwise none.
AM-2 — Acknowledgment
FieldDetailTriggerCreator clicks the acknowledgment link embedded in the brief email.ActionAcknowledgment route updates the brief. Emits brief.acknowledged event.Clay UXOperator asks "any unacknowledged briefs?" → Clay queries via get_briefs_by_status → returns list with time-since-delivery. No dedicated Clay tool for acknowledgment itself — the creator does that via email link.Dashboard UXBrief cards show Acknowledged / Pending badge. Filter by status. Manual override button exists for operators who confirm acknowledgment through other channels (DM, call).DB writescontent_briefs.completion_status → acknowledged, content_briefs.acknowledged_at → NOW(). platform_events row with event_type = 'brief.acknowledged'.Failure handlingIf the creator never clicks the link, the brief sits in delivered state until AM-3's nudge fires. If they acknowledge via manual operator override, the event is emitted with actor_type = 'operator' rather than actor_type = 'creator'.STATUSPARTIAL. What exists: DB columns, /api/brief-acknowledge/[briefId]/route.ts handles the email-link click, send-brief.ts:151 embeds the ackUrl in outgoing email, Dashboard has a manual override button. What doesn't exist: no Clay tool for the operator-side acknowledgment query (only the generic get_briefs_by_status tool covers it indirectly). Audit §3.1 flagged the v1 doc as incorrect on "no creator link" — the link IS embedded.DISTANCEHours. The missing piece is a dedicated Clay read tool surfacing unacknowledged briefs in a more readable shape than get_briefs_by_status (e.g., "which briefs are stale?"). Everything else is in place.DEPENDENCIESLayer 2 (Event Log) for emission. Otherwise none.
AM-3 — Non-Response Follow-Up
FieldDetailTriggerManual path: operator invokes nudge_creator from Clay or clicks Nudge button on Dashboard. Auto path: scheduled function detects a brief in delivered state for > 24 hours with no acknowledgment and fires nudge automatically.ActionSystem sends a follow-up nudge email to the creator, increments nudge_count, stamps last_nudged_at. Emits brief.nudged event. Rate-limited: the same brief cannot be nudged more than once per 24 hours regardless of path.Clay UXOperator: "Luna hasn't acknowledged her brief in 26 hours — nudge her." Clay proposes propose_nudge_creator, operator confirms, Clay calls nudge_creator → "Nudge sent. She's been nudged once this cycle."Dashboard UXBrief card shows amber "Nudge Sent" badge with timestamp and count. Nudge button disabled during the 24-hour rate-limit window.DB writescontent_briefs.last_nudged_at → NOW(), content_briefs.nudge_count → increment. platform_events row with event_type = 'brief.nudged' and actor_type distinguishing manual vs auto.Failure handlingIf SMTP fails: nudge counter does NOT increment; operator alerted. Rate-limit check happens before the email is attempted, so a retry within 24h is blocked regardless of SMTP outcome. Escalation: if nudge_count >= 2 and still no acknowledgment 48 hours after the last nudge, the brief appears in the operator's "at risk" surface for manual intervention.STATUSPARTIAL. What exists: DB columns (last_nudged_at, nudge_count added in 20260417_phase1_action_scaffolding.sql:61-62), Clay tool pair (nudge_creator / propose_nudge_creator) at src/lib/agent/handler-adapters.ts:58 with 24h rate-limit at src/lib/clay/action-handler.ts:684, Dashboard Nudge button wired. What doesn't exist: the auto-trigger. Audit §3.1 notes "Auto-trigger absent from vercel.json" and §7 confirms "Critical agency-facing auto-behaviors (AM-3, OB-3, PM-5) NOT BUILT."DISTANCEDays. The manual path is BUILT. The auto-trigger is one scheduled function that queries content_briefs WHERE delivery_status = 'delivered' AND completion_status = 'delivered' AND delivered_at < NOW() - INTERVAL '24 hours' AND (last_nudged_at IS NULL OR last_nudged_at < NOW() - INTERVAL '24 hours') and calls the existing nudge handler per row. 1-2 days to write, test, add to vercel.json, and wire cron-failure alerting.DEPENDENCIESManual path: Layer 2 (Event Log). Auto path: Layer 2 + Layer 6 (Managed Autonomy). Auto-trigger is BLOCKED until Phase F. The manual path can ship as part of Phase B independently.
AM-4 — Production Tracking
FieldDetailTriggerCreator marks their own brief as "in production" via a future creator portal, OR operator marks it on their behalf via Clay or Dashboard. Today, only the operator path exists.ActionBrief advances through states: delivered → acknowledged → in_production → published. Each transition validates that the new state is a legal next-state from the current state. Emits brief.status_changed event with old and new values.Clay UX"Mark Jake's GLP-1 brief as in production" → Clay proposes propose_update_brief_status, operator confirms, Clay calls update_brief_status → "Done." "What's in production right now?" → Clay calls get_briefs_by_status(status='in_production') → returns list.Dashboard UXStatus dropdown on each brief card. Kanban view (stretch goal, part of Phase I). Status badges color-coded by state.DB writescontent_briefs.completion_status → new status, corresponding timestamp column set (acknowledged_at, in_production_at, published_at). platform_events row with event_type = 'brief.status_changed', payload { from, to }.Failure handlingInvalid transitions (e.g., jumping straight from delivered to published without passing through intermediate states) are rejected at the API layer. Operator sees an error with the allowed next-states. Database-level CHECK constraint on completion_status prevents garbage values.STATUSBUILT. Evidence: audit §3.1 marks this COMPLETE and notes the v1 doc was outdated. Clay tool pair update_brief_status / propose_update_brief_status at src/lib/agent/handler-adapters.ts:88. Dashboard status dropdowns wired. Post-Phase A2, platform_events emission is part of the state-change path.DISTANCEAlready BUILT. The kanban view is a stretch goal that lives under PM-1 in Phase I, not AM-4.DEPENDENCIESLayer 2 (Event Log). Otherwise none.
AM-5 — Publication Confirmation
FieldDetailTriggerCreator pastes published URL into a future creator portal, OR operator logs it via Clay or Dashboard, OR (future) TikTok API auto-detection. Today, only the operator path exists.ActionBrief marked published. published_url stored. The measurement window opens — AM-5 hands off to PA-1 (Performance Data Capture) which begins the 24h/48h/7d measurement cycle. Emits brief.published event.Clay UX"Log Luna's video — https://tiktok.com/…" → Clay proposes propose_update_brief_status with new_status = 'published' and published_url parameter, operator confirms → "Got it. VPS prediction was 42K. I'll track actuals and report back at the 24h mark."Dashboard UX"Mark Published" button with URL input field on the brief card at DashboardClient.tsx:757–802.DB writescontent_briefs.completion_status → published, content_briefs.published_at → NOW(), content_briefs.published_url → URL. platform_events row with event_type = 'brief.published', payload includes the URL and the VPS prediction captured at publish time for later delta calculation.Failure handlingURL validation at the API layer rejects malformed input. If the URL is valid but points to content that's later taken down, performance measurement (PA-1) handles that downstream — AM-5's job ends at confirming the brief reached "published." Re-publishing (e.g., creator reposts after deletion) updates the URL and re-stamps published_at; the event log preserves both.STATUSBUILT. Evidence: audit §3.1 marks this COMPLETE. Wired via update_brief_status with new_status='published' plus published_url parameter. /api/brief-status/route.ts:120 handles the write. Dashboard input field exists. Post-Phase A2, emission to platform_events is in place.DISTANCEAlready BUILT. The TikTok API auto-detection path is deferred to PM-4 in Phase I.DEPENDENCIESLayer 2 (Event Log). Otherwise none. AM-5's output is the trigger for PA-1, so PA-1's completeness depends on AM-5 — not the other way around.
Build gaps summary
WorkflowSchemaAPIDashboardClay toolAuto-triggerSTATUSAM-1 Brief Delivery✅✅✅✅ (via approve_brief)✅BUILTAM-2 Acknowledgment✅✅✅⚠️ (generic read tool only)✅ (email link)PARTIAL — dedicated Clay read tool missingAM-3 Non-Response Follow-Up✅✅✅✅ (manual)❌ (blocked on Phase F)PARTIAL — auto-trigger missingAM-4 Production Tracking✅✅✅✅N/ABUILTAM-5 Publication Confirmation✅✅✅✅N/A (TikTok API deferred to PM-4)BUILT
Tool Call Registry contribution (Account Manager)
ToolKindWrite pairPurposeSTATUSapprove_briefWritepropose / realApproves a brief, fires AM-1 deliveryBUILTget_briefs_by_statusReadn/aQuery briefs by completion_status, used by AM-2 and AM-4BUILTnudge_creatorWritepropose / realManual nudge for AM-3BUILTupdate_brief_statusWritepropose / realState transitions for AM-4 and AM-5BUILTget_unacknowledged_briefsReadn/aDedicated AM-2 read — time-since-delivery viewNOT BUILT (Phase B adds)
Phase B exit criteria
Phase B is complete when:

AM-2's dedicated Clay read tool (get_unacknowledged_briefs) exists and is registered.
AM-3's auto-trigger scheduled function is written, tested, and merged — but NOT yet added to vercel.json (that happens in Phase F).
All five AM workflows emit their documented platform_events types on every mutation path.
Every AM workflow passes a round-trip test: action taken in Clay → state visible in Dashboard, and action taken in Dashboard → state visible in Clay.
A docs/ai-employees/account-manager.md file exists summarizing the five workflows, their current STATUS, and their Phase E refactor plan.

Phase B does NOT ship AM-3's auto-trigger live — that waits for Phase F. It does ship the code.
END OF AI EMPLOYEE 1 — ACCOUNT MANAGER.


AI EMPLOYEE 2 — ONBOARDING SPECIALIST
Role summary
The Onboarding Specialist owns the front of the lifecycle: INVITE → ONBOARD → PROFILE. From the moment an operator decides to add a new creator to their roster, the Onboarding Specialist is responsible for getting that creator invited, walking them through signup, detecting when they stall, sending nudges specific to where they're stuck, and — once they finish — automatically building their profile and fingerprint so they're immediately operational. The creator is fully onboarded when they appear in the roster with a complete profile, a DPS baseline, and a staging path. The handoff to the Account Manager happens at that moment.
What it replaces
The human at an agency who sends welcome emails, walks new creators through setup, follows up when they stall at "connect your TikTok," and manually builds creator profile records in spreadsheets or admin panels. At a growth-phase agency onboarding 2-5 new creators per week, this is roughly 3-6 hours/week of hand-holding plus another 2-3 hours of profile-building per new creator. The profile-building portion is the highest-leverage work to displace — a human doing it produces inconsistent, partial records; an automated pipeline produces the complete 58-feature fingerprint + DPS baseline + staging path every time.
Substrate assumption
This section is written assuming Phase A is complete AND Phase B is complete. That means:

Layer 0 (Data Ingestion): BUILT
Layer 1 (Scoring Engine): BUILT
Layer 2 (Event Log): BUILT via Phase A2. Onboarding workflows emit to platform_events on every state change.
Layer 3 (Memory): PARTIAL — Phase D completes this. OB workflows do not depend on Phase D.
Layer 4 (Creator Operations Graph): BUILT via Phase A3. onboarding_profiles is canonical. This layer is why Phase C cannot start before Phase A3. Every OB workflow writes to or reads from the canonical creator record; running them before consolidation would either (a) write into the wrong table and produce phantom creators, or (b) read from an out-of-sync projection and show stale onboarding state. Audit §7 names this dependency explicitly.
Layer 5 (Capability Surfaces): PARTIAL — Phase E completes this. Per the Phase B technical-debt callout, OB workflows inherit the same asymmetric-pattern debt and get refactored in Phase E.
Layer 6 (Managed Autonomy): PARTIAL — Phase F completes this. OB-3's auto-trigger and OB-4's auto-trigger both require Phase F; their manual/synchronous equivalents do not.

One additional assumption carried over from Phase B: send_invite — stubbed in the current codebase (audit §3.2: "DB row created, email NOT SENT") — is unstubbed as part of Phase C's OB-1 work. The email delivery pipeline that AM-1 uses for brief delivery is the same SMTP path; OB-1 just needs to invoke it with invitation content instead of brief content.
Per-workflow detail
OB-1 — Invitation
FieldDetailTriggerOperator invites a new creator (in Clay: "invite jake@email.com as a fitness creator with 50K followers"; in Dashboard: Invite Creator form).ActionSystem creates an agency_invites row, then sends an invitation email via SMTP containing a signup link with an embedded invite token. Emits creator.invited event.Clay UX"Invite jake@email.com — fitness creator, 50K followers" → Clay proposes propose_invite_creator, operator confirms, Clay calls invite_creator → "Invitation sent. I'll track his onboarding and alert you if he stalls."Dashboard UX"Invite Creator" button opens a form (email, name, niche, optional notes). Submit → row created + email sent. Pending invites appear in an "Invited, not yet accepted" list with sent timestamp.DB writesagency_invites row — agency_id, creator_email, creator_name, invited_by (session user), invited_at NOW(), status = 'sent', sent_at NOW(). If SMTP fails: status = 'failed', error_message populated. platform_events row with event_type = 'creator.invited', payload { invite_id, agency_id, email, niche }.Failure handlingIf SMTP fails: invite row persists with status = 'failed' and error_message; operator can retry from Dashboard or re-invoke the Clay tool. Retry is idempotent on (agency_id, creator_email) — an existing pending or sent invite updates in place rather than creating a duplicate. Invites expire after 14 days (status = 'expired') via the same scheduled function that handles OB-3 stall detection.STATUSPARTIAL. What exists: agency_invites schema (20260417_phase1_action_scaffolding.sql:10) with status CHECK covering pending/sent/accepted/declined/expired/failed, handler case sendInvite at src/lib/clay/action-handler.ts:656 that inserts the invite row, ActionButton path that dispatches to this handler. What doesn't exist: (1) the email is never actually sent — audit §3.2 confirms "DB row created, email NOT SENT" and action-handler.ts:675-681 explicitly stubs delivery with the comment "Delivery pipeline ships in Turn 4"; (2) no registered Clay adapter (propose/real pair) — OB-1 goes through ActionButton only, outside the proposal-gated tool path; (3) table-name collision where agency-chat/route.ts:457 queries the nonexistent plural agency_invitations and returns empty silently (audit §6.F).DISTANCEDays. Three concrete gaps: (1) unstub the SMTP send — reuse AM-1's src/lib/email/send-brief.ts SMTP path with an invitation template, ~half a day; (2) register invite_creator / propose_invite_creator in handler-adapters.ts following the nudge_creator pattern, ~half a day; (3) fix the agency_invitations → agency_invites query in agency-chat/route.ts:457, ~1 hour. Total: 1-2 days.DEPENDENCIESLayer 2 (Event Log), Layer 4 (Creator Operations Graph — so agency_id resolution is trustworthy). Phase B is a soft dependency only because OB-1's SMTP code reuses AM-1's path — if Phase B shipped that path cleanly, OB-1's SMTP work is ~4 hours instead of a day.
OB-2 — Onboarding Progress Tracking
FieldDetailTriggerAccept path: creator clicks the invite link and begins signup — an onboarding_profiles row is created with onboarding_step = 'started'. Progress path: creator advances through onboarding steps (account created → niche selected → TikTok linked → calibration completed → onboarding completed); each step updates onboarding_step and creator_stage. Read path: operator asks Clay or views Dashboard pipeline to see where each invited creator is.ActionOn accept: system creates the onboarding_profiles row, marks the agency_invites row as accepted, emits creator.onboarding_started. On each progress step: system updates onboarding_step, creator_stage, and the relevant stage-specific column (e.g., channel_handle when TikTok is linked), emits creator.onboarding_progressed with { from_step, to_step }.Clay UX"How's Jake's onboarding going?" → Clay calls get_onboarding_status → "Jake created his account 2 days ago but hasn't linked his TikTok yet. He's at step 3 of 5. Want me to nudge him?" "Who's stalled in onboarding?" → Clay calls get_onboarding_status(filter='stalled') → returns list with time-at-current-step.Dashboard UXOnboarding pipeline page — creators grouped in columns by onboarding_step (Invited → Started → TikTok Linked → Calibration → Completed). Progress bar per creator showing step-of-total. Time-at-current-step visible on hover. "Stalled" creators (>48h at same step) highlighted amber.DB writesonboarding_profiles.onboarding_step, creator_stage (e.g., pre_calibration / calibrating / staged), onboarding_completed BOOL when finished, plus whichever step-specific column the advance populated. agency_invites.status → accepted, accepted_at NOW() on the initial acceptance. platform_events rows with event_type in {'creator.onboarding_started', 'creator.onboarding_progressed', 'creator.onboarding_completed'}.Failure handlingIf a creator accepts an invite that's already expired, the accept is rejected with a clear error; operator can re-invite. If two invites exist for the same email (e.g., operator sent a second one), the accept path uses the most recently-sent valid invite. If progress-step writes fail mid-flow (network drop during calibration), the Event Log preserves the partial state; creator resumes from their last persisted onboarding_step on next login.STATUSPARTIAL. What exists: creator_stage column on onboarding_profiles (20260322_merge_calibration:35), onboarding_step column, agency-chat reads stages passively when assembling context (agency-chat/route.ts:629–658). What doesn't exist: (1) dedicated operator-facing pipeline page — audit §3.2 confirms "no dedicated page"; (2) no Clay read tool (get_onboarding_status) — operators cannot ask about onboarding state via Clay; (3) no platform_events emissions on stage transitions (pre-Phase A2 this was acceptable; post-Phase A2 it is a gap OB-2 must close).DISTANCEDays. Three concrete pieces: (1) register get_onboarding_status Clay read tool following the get_briefs_by_status pattern, ~half a day; (2) add platform_events emission on every onboarding_step / creator_stage write (including the implicit writes from existing onboarding flows), ~1 day; (3) Dashboard pipeline page — column-grouped view with stall highlighting, ~1-2 days. Total: 2-3 days.DEPENDENCIESLayer 2 (Event Log), Layer 4 (Creator Operations Graph — onboarding_profiles must be canonical for stage reads to be trustworthy). OB-1 must ship first so there are invited creators to track progress for.
OB-3 — Stall Detection & Auto-Nudge
FieldDetailTriggerManual path: operator invokes nudge_onboarding from Clay or clicks Nudge button on the Dashboard pipeline page for a specific stalled creator. Auto path: scheduled function (every 6 hours) detects creators at the same onboarding_step for > 48 hours and fires a nudge specific to their stalled step.ActionSystem sends a follow-up nudge email whose content matches the stalled step: "You're almost there — link your TikTok" for stage-3 stalls, "Quick calibration — 3 minutes" for stage-4 stalls, etc. Increments nudge_count, stamps last_nudged_at, updates nudge_stage to record which step's nudge was sent. Emits creator.onboarding_nudged event. Rate-limited: no more than one nudge per creator per 24 hours, maximum 2 automated nudges total per onboarding_step before escalation.Clay UXOperator: "nudge Marcus about his onboarding" → Clay proposes propose_nudge_onboarding, operator confirms → "Nudge sent. He's stalled at 'link TikTok' — this was his second nudge for that step. One more no-response and I'll flag him for your direct outreach." Passive: Clay's morning briefing surfaces stalled creators: "2 creators stalled in onboarding. Marcus: 3 days at 'link TikTok', nudged twice. Luna: 50 hours at 'calibration', not yet nudged."Dashboard UXOn the OB-2 pipeline page: stalled creator cards show amber "Stalled Nh" badge with nudge history on hover. Manual Nudge button with the current nudge-count and rate-limit state (disabled during 24h rate-limit window). "At Risk" section at the top listing creators who have exhausted automated nudges and need operator outreach.DB writesagency_invites.nudge_count (increment), agency_invites.last_nudged_at NOW(), agency_invites.nudge_stage (the step nudged about). platform_events row with event_type = 'creator.onboarding_nudged', payload `{ invite_id, stalled_step, nudge_number, actor_type: 'operator'Failure handlingSMTP failure: nudge counter does NOT increment, error logged, operator alerted. Rate-limit check happens before the email attempt, so a retry within 24h is blocked regardless of SMTP outcome. If a stalled creator completes their step between when the auto-trigger queues their nudge and when SMTP actually sends, the pre-send check re-verifies the stall and aborts the nudge (idempotency). If the operator manually nudges within 1 hour of an auto-nudge, the manual nudge is treated as a no-op with a clear message ("auto-nudge already sent 20min ago").STATUSNOT BUILT. What exists: nothing. Audit §3.2 confirms "No schema, no cron, no logic." What doesn't exist: (1) the schema — the doc previously referenced invitations.nudge_count / last_nudge_at / nudge_stage, none of which exist on the real table (audit §1.C.4, §6.A); (2) the manual nudge handler (OB-3 has no analog to AM-3's nudge_creator — it needs a distinct nudge_onboarding because the recipient is a prospect on agency_invites, not a signed-in creator on onboarding_profiles); (3) the auto-trigger; (4) the escalation path; (5) the "at risk" surface. Audit §7 names OB-3 alongside AM-3 and PM-5 as "Critical agency-facing auto-behaviors NOT BUILT."DISTANCEWeeks. Five pieces: (1) add nudge_count, last_nudged_at, nudge_stage columns to agency_invites with a migration — half a day; (2) write stage-specific email templates (one per onboarding_step that can stall), ~1 day; (3) build the manual nudge_onboarding Clay adapter + Dashboard button with 24h rate-limiting, ~1-2 days; (4) write the auto-trigger scheduled function (query stalled invites, dispatch nudges, respect rate limits), ~2 days; (5) build the "At Risk" Dashboard surface + the Clay morning-briefing surfaces + the escalation event emission, ~2-3 days. Total: 1-2 weeks.DEPENDENCIESLayer 2 (Event Log), Layer 4 (Creator Operations Graph), Layer 6 (Managed Autonomy) for the auto-trigger. OB-1 must ship first (SMTP path and invite rows must exist). OB-2 must ship first (stage-tracking data is the input signal for stall detection). Auto-trigger is BLOCKED until Phase F. The manual path + the new schema can ship as part of Phase C independently.
OB-4 — Auto-Profiling
FieldDetailTriggerPrimary path: creator links their TikTok account during onboarding (completes onboarding_step = 'tiktok_linked'). A background job is enqueued to run the profiling pipeline. Manual path: operator invokes rebuild_creator_profile from Clay or clicks Rebuild Profile on the Dashboard creator card.ActionSystem runs the profiling pipeline on the creator's recent TikTok content: scrapes recent videos, extracts features, computes the fingerprint (see Note below), calculates DPS baseline, determines staging path (pre_calibration → calibrating → staged). Writes results to the canonical creator record on onboarding_profiles. Emits creator.profile_built event. On completion, advances onboarding_step if all other gating steps are done, which can trigger the terminal creator.onboarding_completed event.Clay UXPassive: when Jake finishes onboarding, Clay's morning briefing includes: "Jake just completed onboarding. His profile shows strong hook retention but weak audience clarity. Staging path: pre_calibration. Ready for his first brief." On demand: "rebuild Luna's profile" → Clay proposes propose_rebuild_creator_profile, operator confirms → "Rebuilt. DPS baseline updated from 68 to 74. Her fingerprint shifted slightly — declarative hooks strengthened."Dashboard UXCreator card on the roster page shows profile, DPS, fingerprint summary, and staging path as soon as profiling completes. For in-progress profiling: "Building profile..." indicator. Manual Rebuild Profile button on the creator detail page. Profile-build history visible (timestamps of each rebuild).DB writesonboarding_profiles updated with dimension_scores (JSONB), inferred_niche, differentiator, creator_stage, calibration_completed. Via the Phase F rebuild job (deferred), creator_profiles is updated as the stale projection — but during Phase C, OB-4 writes ONLY to onboarding_profiles per Layer 4's consolidation rule (audit §1.C.4 + Phase A3 exit criteria prohibit new writes to creator_profiles). platform_events row with event_type = 'creator.profile_built', payload `{ creator_id, dps_baseline, staging_path, trigger: 'auto'Failure handlingIf TikTok scraping fails (rate limit, account private, no recent posts): profile build marked failed with a specific reason; operator sees actionable message ("Luna's TikTok has no posts from the last 30 days — can't compute a DPS baseline yet"). Partial success (some features extracted, others failed) is acceptable — the profile ships with available data and calibration_completed = false, which signals downstream AI Employees (Account Manager, Performance Analyst) to treat the creator's DPS baseline as provisional. Retry is idempotent — a second run replaces, not accumulates.STATUSPARTIAL. What exists: rebuildCreatorProfile() at src/lib/creator/profile_builder.ts:50 — audit §3.2 confirms the function exists. What doesn't exist: (1) the auto-trigger on TikTok-link event — audit notes "no auto-trigger on TikTok link"; (2) no Clay adapter (rebuild_creator_profile / propose_rebuild_creator_profile); (3) no Dashboard Rebuild Profile button; (4) the function currently writes to creator_profiles (audit §3.2 confirms), which violates Layer 4's post-A3 consolidation rule — OB-4's work includes pointing the function at onboarding_profiles instead. Separate from the wiring issue, the underlying fingerprint implementation is much weaker than v1 claimed. Audit §10 flags this: SKILL-001 ("Creator Profiling") is a 32-element character-frequency hash as style_embedding, not the "58-feature fingerprint" v1 described. OB-4 as specced here wires up the existing weak fingerprint; replacing the fingerprint itself is a separate future workstream tied to SKILL-002.DISTANCEDays. Four pieces: (1) point rebuildCreatorProfile() at onboarding_profiles instead of creator_profiles, respecting the Layer 4 consolidation rule, ~half a day; (2) add the auto-trigger — when onboarding_step advances to 'tiktok_linked', enqueue profile build (synchronous if fast, background if slow), ~1 day; (3) register rebuild_creator_profile Clay adapter + Dashboard button, ~1 day; (4) add platform_events emission + failure surfacing, ~half a day. Total: 2-3 days. Does NOT include a fingerprint rewrite — that's downstream.DEPENDENCIESLayer 0 (Data Ingestion — scraping pipeline), Layer 1 (Scoring Engine — DPS computation), Layer 2 (Event Log), Layer 4 (Creator Operations Graph — writes go to onboarding_profiles). OB-2 must ship first (the 'tiktok_linked' step transition is the trigger signal). The fingerprint-rewrite future work is blocked on SKILL-002 unblocking, per audit §3.6 dependency map — but OB-4 itself does not need that rewrite to ship.
Build gaps summary
WorkflowSchemaAPIDashboardClay toolAuto-triggerSTATUSOB-1 Invitation✅⚠️ (SMTP stubbed)✅ (button exists, email silent)⚠️ (ActionButton only, no adapter)✅ (on operator submit)PARTIAL — email never sent, no registered adapterOB-2 Progress Tracking✅ (columns exist)⚠️ (passive reads only, no emissions)❌ (no pipeline page)❌✅ (implicit on step writes)PARTIAL — no operator-facing surfaceOB-3 Stall Detection & Auto-Nudge❌ (columns missing)❌❌❌❌ (blocked on Phase F)NOT BUILTOB-4 Auto-Profiling✅ (writes to wrong table)⚠️ (function exists, writes to creator_profiles)❌❌❌PARTIAL — manual invocation only, target table wrong
Tool Call Registry contribution (Onboarding Specialist)
ToolKindWrite pairPurposeSTATUSinvite_creatorWritepropose / realOB-1 — send invitation emailNOT BUILT (handler exists but no adapter, email stubbed)get_onboarding_statusReadn/aOB-2 — query creators by onboarding_step / creator_stageNOT BUILTnudge_onboardingWritepropose / realOB-3 — manual nudge to stalled prospectNOT BUILTrebuild_creator_profileWritepropose / realOB-4 — manual profile rebuildNOT BUILT
Phase C exit criteria
Phase C is complete when:

OB-1 sends actual emails — the send_invite stub at action-handler.ts:675-681 is removed, the SMTP path is live, and a test invitation delivers end-to-end to an external inbox.
OB-1, OB-3, and OB-4 all have registered Clay adapters (propose/real pairs) in handler-adapters.ts, not ActionButton-only paths.
The agency_invitations (plural) typo at agency-chat/route.ts:457 is fixed to agency_invites.
OB-2's Dashboard pipeline page exists and renders every invited creator grouped by onboarding_step with stall highlighting.
OB-3's schema migration (nudge_count, last_nudged_at, nudge_stage on agency_invites) is merged; the manual nudge path ships; the auto-trigger scheduled function is written and tested but NOT yet added to vercel.json (that happens in Phase F, same discipline as AM-3).
OB-4's auto-trigger fires on onboarding_step → 'tiktok_linked' transitions, and rebuildCreatorProfile() writes to onboarding_profiles, not creator_profiles — verified by running the six Phase A3 verification greps again and confirming no new creator_profiles writes were introduced.
All four OB workflows emit their documented platform_events types on every mutation path.
Every OB workflow passes a round-trip test: action taken in Clay → state visible in Dashboard, and action taken in Dashboard → state visible in Clay.
A docs/ai-employees/onboarding-specialist.md file exists summarizing the four workflows, their current STATUS, their Phase E refactor plan, and the separately-tracked SKILL-002 fingerprint-rewrite dependency for OB-4's deeper work.

Phase C does NOT ship OB-3's auto-trigger live — that waits for Phase F, same as AM-3. It does ship the code. Phase C also does NOT rewrite the fingerprint implementation — OB-4 wires up what exists and flags the fingerprint rewrite as a downstream workstream.
END OF AI EMPLOYEE 2 — ONBOARDING SPECIALIST.


AI EMPLOYEE 3 — TREND SCOUT / CONTENT STRATEGIST
Role summary
The Trend Scout owns the feature that makes Trendzo addictive rather than useful. It continuously scans the cultural signal landscape — TikTok trending, Reddit, X, news — for topics spiking in each agency's creator niches, matches those topics to specific creators based on fingerprint fit and past performance, auto-generates draft briefs when match confidence is high, and alerts creators when trends matching their niche emerge. Account Manager, Onboarding Specialist, and Performance Analyst all operate on briefs and creators that already exist; the Trend Scout is the only AI Employee that creates opportunities proactively. Unlike every other employee in the lifecycle, it does not wait to be invoked — it runs on its own, and when it surfaces a match, the rest of the lifecycle begins.
What it replaces
The human at an agency who scrolls TikTok for 3 hours a day looking for trends, cross-references them against a creator roster to figure out who should jump on what, drafts briefs from scratch against fuzzy topic notions, and sends "hey, this is trending in your space" pings to creators. At a 10-creator agency this is roughly 15-20 hours/week of scrolling + matching + drafting; at a 40-creator agency it is a full role. The highest-leverage displacement is not the scrolling itself — it is the matching, because a human matcher can hold maybe 8-10 creator fingerprints in working memory at once, while an automated matcher can score every creator against every trend every time one appears.
Substrate assumption
This section is written assuming Phases A, B, and C are complete, and the Trend Scout sits on top of Phases D, E, and F. Specifically:

Layer 0 (Data Ingestion): BUILT
Layer 1 (Scoring Engine): BUILT
Layer 2 (Event Log): BUILT via Phase A2. Trend detection, match computation, auto-brief generation, and creator alerting all emit to platform_events.
Layer 3 (Memory): BUILT via Phase D. The Trend Scout reads memory to bias matching (e.g., "Luna has previously rejected trend-triggered briefs about politics" → lower match confidence for politically-adjacent trends).
Layer 4 (Creator Operations Graph): BUILT via Phase A3. Matching reads creator fingerprints from onboarding_profiles. This layer is non-negotiable — matching against three parallel creator tables produces three different match results.
Layer 5 (Capability Surfaces): BUILT via Phase E. Unlike Phases B and C which inherited the asymmetric-pattern debt, Phase G ships natively against the unified registration. No Phase-E refactor is ever needed for Trend Scout workflows.
Layer 6 (Managed Autonomy): BUILT via Phase F. This is the other non-negotiable — the entire Trend Scout is defined by continuous autonomous operation. Running it on human triggers defeats the purpose.

Why Phase G is sequenced after D, E, AND F, not just after A-B-C: Every prior AI Employee can ship a meaningful manual-only version. Trend Scout cannot. A manual-only Trend Scout is an operator scrolling TikTok and typing topics into Clay — which is exactly the human work this employee displaces. Phase G depends on managed autonomy not because the code needs it, but because the feature is autonomy. Audit §7 makes this explicit: "Critical agency-facing auto-behaviors" are blocked until Layer 6 is BUILT.
Per-workflow detail
TS-1 — Continuous Trend Monitoring
FieldDetailTriggerScheduled: cultural-scan cron runs every 1-6 hours (config-driven per environment; production default is every 2 hours). Manual: operator invokes refresh_trends from Clay or clicks "Scan now" on the Dashboard trend radar — a privileged action (rate-limited to once per 15 minutes per agency) that forces an out-of-cycle scan.ActionSystem scans configured sources (TikTok trending, Reddit sub-feeds, X, RSS news) and pipes raw signal through the LLM synthesizer at /api/cron/cultural-scan/route.ts. Raw synthesized topics land in detected_trends. A classification pass promotes high-confidence topics to cultural_events with taxonomy tags, velocity scores, confidence scores, activated niches, and predicted expiry windows. Emits trend.detected and trend.promoted events.Clay UXPassive: Clay's morning briefing leads with the trends that emerged overnight. "3 trends spiked in your niches overnight. Fitness: GLP-1 shortage panic (velocity: high, window: 48hrs). Finance: Fed rate speculation (velocity: medium, window: 5 days). Health: sleep optimization resurfaced (velocity: low but rising). Want details on any?" On demand: "what's trending in my niches right now?" → Clay calls get_trends(niche_filter=agency_niches) → returns ranked list.Dashboard UXTrend Radar page at /agency/trends/ renders real-time trend cards sorted by velocity × relevance. Each card shows title, taxonomy, velocity score, confidence, predicted window, and a small sparkline of velocity-over-time. Niche filter chips at the top. "Scan now" button with rate-limit indicator.DB writesdetected_trends — raw LLM-synthesized output, one row per candidate topic. cultural_events — promoted, classified trends with velocity_score, confidence, activated_niches[], status = 'detected', expires_at. platform_events rows with event_type in {'trend.detected', 'trend.promoted', 'trend.expired'}, payload includes velocity and niche tags.Failure handlingIf a source is unavailable (TikTok rate-limiting, Reddit 503): scan continues on other sources; the missing source is logged as a partial-scan event with the error surfaced in the operator alert inbox only if it fails 3 consecutive runs. If the LLM synthesizer returns unclassifiable output: rows land in detected_trends but do not promote to cultural_events; they expire on schedule. If the cron itself fails to run: Layer 6's cron-failure alerting (Phase F deliverable) surfaces the missed run. Idempotency: the promotion pass deduplicates against recent cultural_events — a trend that was already promoted in the last 24 hours updates velocity on the existing row rather than creating a duplicate.STATUSPARTIAL. What exists: cultural_events schema (20260407_create_cultural_events.sql:5, extended 20260407_agent_attribution.sql:7), detected_trends schema (20260407_create_detected_trends.sql:4), the cultural-scan cron route at /api/cron/cultural-scan/route.ts, the Trend Radar page at /agency/trends/. What doesn't exist: (1) the cron is not scheduled in vercel.json — audit §3.3 and §7 both confirm this; (2) the Trend Radar page uses scraped_videos + hardcoded fallback data rather than reading from detected_trends / cultural_events — audit §3.3 flags trends/page.tsx:56–76 with Math.random() at line 72 fabricating velocity in a server component; (3) the FOMO section hardcodes "12 agencies" / "3.2x higher engagement" — operator-facing fiction that must be removed before TS-1 can be called BUILT; (4) no Clay read tool (get_trends); (5) no manual refresh_trends with rate-limiting; (6) no platform_events emissions.DISTANCEDays. Six concrete pieces: (1) add the cron entry to vercel.json (Phase F already does this generally, but TS-1 specifically needs the entry verified), ~1 hour; (2) rewrite trends/page.tsx to read from cultural_events instead of scraped_videos, delete all Math.random() and hardcoded FOMO strings, ~1 day; (3) register get_trends Clay read tool, ~half a day; (4) register refresh_trends Clay adapter (propose/real pair) + Dashboard "Scan now" button with rate-limiting, ~1 day; (5) add platform_events emission on detection, promotion, and expiry, ~half a day; (6) wire up Layer 6's cron-failure alerting to the Trend Radar page so operators see "last scan: 4h ago" and get alerted when it's stale, ~half a day. Total: 3-4 days.DEPENDENCIESLayer 2 (Event Log), Layer 5 (Capability Surfaces — shipping natively to the unified registration, no Phase E refactor), Layer 6 (Managed Autonomy — the cron entry and failure alerting).
TS-2 — Trend-to-Creator Matching
FieldDetailTriggerNew trend is promoted to cultural_events (emits trend.promoted event); matching pass runs in response. Or: operator asks "which of my creators match this trend?" in Clay, or clicks "Find matches" on a trend card in the Dashboard.ActionSystem scores every active creator in the agency against the new trend. Match score is a function of: (a) fingerprint fit — how well the creator's content DNA aligns with the trend's taxonomy, (b) niche alignment — direct, adjacent, or stretch, (c) historical performance on similar trends — does the creator tend to hit on this kind of topic, (d) memory bias — does warm memory say "creator prefers/rejects this kind of topic." Writes one row per (trend, creator) pair above a threshold to trend_creator_matches. Emits trend.matched events.Clay UXPassive: "The GLP-1 trend matches 3 of your creators. Luna: 92% — she's done health content before and her declarative hooks fit this taxonomy. Jake: 71% — adjacent niche, fingerprint overlap is weaker. Marcus: 45% — stretch, but his audience skews health-curious. Want me to draft briefs for the top two?" On demand: "which creators match the Fed rate trend?" → Clay calls get_trend_matches(trend_id) → returns ranked list with match_score + one-sentence rationale per creator.Dashboard UXOn each trend card in the Trend Radar: a "Matched Creators" section showing creator avatars with match scores on hover, sorted high-to-low. Click a creator avatar → opens match detail with contributing factors (fingerprint fit breakdown, niche alignment, historical precedent, memory-derived biases).DB writesRequires new table: trend_creator_matches — columns: id, trend_id FK → cultural_events, creator_id FK → onboarding_profiles, agency_id FK → agencies, match_score NUMERIC, match_reason JSONB (contributing factors), threshold_crossed BOOL, created_at. Unique constraint on (trend_id, creator_id). platform_events rows with event_type = 'trend.matched', payload includes match_score and threshold_crossed flag.Failure handlingIf a creator has no fingerprint yet (onboarding incomplete, or OB-4 hasn't run): match is skipped for that creator with a logged reason ("no fingerprint — profile building required"); operator sees the creator listed in a "needs profile" section on the trend card rather than a silent skip. If fingerprint exists but is the weak 32-element hash flagged by audit §10, matches compute but with a lower confidence ceiling — the match_reason JSONB records "fingerprint_type": "legacy_hash" so downstream consumers (TS-3 auto-brief gating) can weight accordingly. Stale matches (trend has expired): expiry cron sets threshold_crossed = false rather than deleting rows, preserving audit history.STATUSNOT BUILT. What exists: nothing dedicated. Audit §3.3 confirms "NOT BUILT" and notes that the existing match_creators_to_event handler matches on niche-string equality against agency_events (operator-defined) — not fingerprint fit against cultural_events (platform-detected), and not producing a persistent matches table. What doesn't exist: (1) the trend_creator_matches table — audit §1.C.4 lists this among the five tables v1 named that do not exist in schema; (2) the matching algorithm — fingerprint fit, niche alignment, historical precedent, memory bias — none of the four signals are computed today; (3) the Clay read tool; (4) the Dashboard matched-creators section on trend cards; (5) the trigger chain from trend.promoted → matching pass.DISTANCEWeeks. Six pieces: (1) create the trend_creator_matches migration with the columns, FKs, unique constraint, and indexes, ~half a day; (2) build the matching algorithm — this is the real work. Four signals × production-quality implementation × tests against historical data = 3-5 days. Fingerprint fit is the heaviest piece and its ceiling is bounded by the underlying fingerprint quality — the audit-flagged weak hash limits TS-2's ultimate accuracy until SKILL-002 replaces it; (3) wire the trigger: trend.promoted event → matching pass → trend_creator_matches insert, ~1 day; (4) register get_trend_matches Clay read tool, ~half a day; (5) add the matched-creators section to trend cards in the Dashboard, ~1-2 days; (6) platform_events emission + the match-detail expandable view with contributing factors, ~1 day. Total: 7-10 days.DEPENDENCIESLayer 2 (Event Log — both as trigger for trend.promoted and as emission target for trend.matched), Layer 3 (Memory — bias signal), Layer 4 (Creator Operations Graph — reads fingerprints from onboarding_profiles), Layer 5 (Capability Surfaces), Layer 6 (Managed Autonomy — the matching pass runs in response to an autonomous trigger, not a human click). TS-1 must ship first (matching against empty cultural_events produces nothing). Underlying accuracy is bounded by fingerprint quality — replacing the weak hash with the real 58-feature fingerprint is tracked as a separate SKILL-002 workstream that does not block TS-2 from shipping, but will materially improve its output when it lands.
TS-3 — Auto-Brief Generation from Trends
FieldDetailTriggerHigh-confidence auto path: a trend_creator_matches row is inserted with match_score above the operator-configured agency threshold (default: 0.85) → auto-generate draft brief. Operator path: operator says "draft a brief for Luna on the GLP-1 trend" in Clay, or clicks "Generate brief" on a trend × creator match in the Dashboard. Batch path: the existing autodream nightly batch continues to generate morning briefs per agency, now seeded by the day's top-scoring trend_creator_matches rows rather than raw cultural_events.ActionSystem auto-generates a draft brief by composing: creator's fingerprint + creator's recent performance patterns + trend's taxonomy and velocity + adversarial evaluation rounds. Writes the draft to pre_generated_briefs with status = 'draft'. Operator reviews via the existing brief-review surface. On approval, the brief graduates to content_briefs and AM-1 takes over. Emits brief.auto_drafted and (on approval) brief.approved_from_trend.Clay UXPassive: "I've drafted a GLP-1 brief for Luna based on her 92% match with the trending topic. VPS prediction: 38K views, confidence 0.72. Want to review it?" → renders TrendzoCard with brief content, predicted performance, and the trend that sourced it. On demand: "draft a GLP-1 brief for Jake" → Clay proposes propose_generate_brief_from_trend with {creator_id, trend_id}, operator confirms → brief generated and surfaced for review.Dashboard UXDraft brief appears in the existing brief review queue with a "Trend-Sourced" tag and the source trend linked. Hovering the tag shows the trend's velocity, window, and the match score that triggered generation. Approve / reject buttons exist where they already do (brief-review surface).DB writespre_generated_briefs row — standard brief row plus cultural_event_id FK → cultural_events (column already exists per 20260407_autodream_tables.sql:6), plus a new trend_match_id FK → trend_creator_matches to preserve the match rationale that produced this brief. On approval: existing flow inserts into content_briefs. platform_events rows with event_type in {'brief.auto_drafted', 'brief.generated_from_trend', 'brief.approved_from_trend', 'brief.rejected_from_trend'}, payload includes trend_id, creator_id, match_score, vps_prediction.Failure handlingIf the brief generator returns low adversarial-critic scores: the draft is still persisted but flagged with final_critic_score < threshold and surfaced in review with a "low confidence" indicator rather than auto-approval. If a trend expires between match and generation (race condition — trend window closed during LLM call): brief is still generated but with a warning that the trend window has closed; operator decides whether to pursue. If the creator has no fingerprint (OB-4 incomplete): generation is blocked with a clear error pointing the operator at the profiling step. Rate-limiting: max 10 auto-generated trend briefs per agency per 24 hours to prevent storm conditions when many trends promote simultaneously.STATUSPARTIAL. What exists: pre_generated_briefs schema with cultural_event_id FK (20260407_autodream_tables.sql:6), the autodream nightly batch at /api/cron/autodream/route.ts that generates briefs from cultural_events, brief generation logic (Gemini 2.5 Flash + adversarial rounds) that works per audit §3.6 ("brief generation works ... autodream batch, trend-triggered"), the generate-brief ActionButton path in the handler at action-handler.ts:103,242. What doesn't exist: (1) no real-time trigger from trend.promoted events — audit §3.3 confirms "no real-time trigger"; (2) no Clay adapter (propose/real pair) — generate_brief is ActionButton-only, outside the proposal-gated tool path per audit §3.3; (3) no link from trend_creator_matches into brief generation (TS-2 must ship first); (4) the autodream cron is not scheduled in vercel.json, same gap as TS-1's cron; (5) no trend_match_id column on pre_generated_briefs; (6) platform_events emissions are inconsistent.DISTANCEDays to Weeks depending on TS-2. Assuming TS-2 ships first: (1) add trend_match_id column to pre_generated_briefs, ~1 hour; (2) register generate_brief_from_trend Clay adapter (propose/real pair) and wire the Dashboard "Generate brief" button, ~1 day; (3) write the real-time trigger listening for trend.matched events above threshold → generate draft, ~1-2 days; (4) schedule the autodream cron in vercel.json, ~1 hour; (5) add rate-limiting, platform_events emission, and the low-critic-score flagging, ~1 day. Total: 3-5 days once TS-2 is BUILT. If TS-2 is not yet BUILT, this workflow inherits BLOCKED status for the auto path (manual path can ship earlier but operates against raw cultural_events without the match rationale — a diminished version).DEPENDENCIESLayer 2 (Event Log), Layer 4 (Creator Operations Graph), Layer 5 (Capability Surfaces), Layer 6 (Managed Autonomy — autodream cron + real-time trigger). TS-1 and TS-2 must both ship first for the full auto path to work. The deeper brief-quality workstream is bounded by the SKILL-002 fingerprint replacement and SKILL-004 packaging, per audit §3.6 — brief generation currently works but does not consume a SKILL-002 fingerprint. TS-3 ships against the current implementation; quality improvements track SKILL-002 independently.
TS-4 — Creator-Facing Trend Alerts
FieldDetailTriggerA trend_creator_matches row is inserted AND that creator has auto-alerts enabled by their agency. Or: operator invokes alert_creator_about_trend manually from Clay or Dashboard, overriding the auto-alert setting.ActionSystem sends a creator-facing notification — in-app if the creator portal is live, otherwise email — along the lines of: "Trending now in your niche: GLP-1 shortage panic. Your agency has a brief ready — check your dashboard" (or: "Your agency will reach out soon" if no brief has been drafted yet). Writes one row per alert to creator_alerts, dedupe-keyed to prevent multiple alerts for the same trend × creator pair within 24 hours. Emits creator.trend_alerted.Clay UXPassive: Clay's morning briefing includes a creator-alerts summary. "Auto-alerts sent to Luna and Jake about the GLP-1 trend. Marcus was matched but auto-alerts are off for him — want me to alert him manually?" On demand: "alert Marcus about the Fed rate trend" → Clay proposes propose_alert_creator_about_trend, operator confirms → alert sent, logged.Dashboard UXAlert log page showing which creators were notified about which trends with timestamps and delivery status (sent / opened / clicked). Per-creator preference settings in the creator detail view: auto-alert toggle, min match score threshold for alerts, quiet hours.DB writesRequires new table: creator_alerts — columns: id, creator_id FK → onboarding_profiles, agency_id FK → agencies, trend_id FK → cultural_events, trend_match_id FK → trend_creator_matches, alert_type TEXT (in-app / email), sent_at, delivery_status, opened_at nullable, clicked_at nullable. Unique constraint on (creator_id, trend_id) with partial index on sends within the last 24 hours (dedupe window). platform_events rows with event_type in {'creator.trend_alerted', 'creator.trend_alert_opened', 'creator.trend_alert_clicked'}.Failure handlingIf the creator has no contact channel configured (no email, no portal access): alert is skipped with a logged reason; operator sees creator in "needs contact" list on the alert log. If auto-alerts are globally disabled for an agency (operator preference): alerts queue as "would-have-sent" events for observability but no delivery occurs. Email bounce: delivery_status → bounced, surfaced to operator. Quiet hours: alert is queued and sent at the end of the quiet window unless the trend window has already closed, in which case it is dropped with a logged reason.STATUSNOT BUILT. What exists: nothing. Audit §3.3 and §1.C.4 both confirm creator_alerts is among the five tables v1 named that do not exist. What doesn't exist: (1) the creator_alerts table; (2) the creator-facing notification channel — the creator portal referenced in audit §3.3 is a stub; (3) any creator email-send path outside the operator-to-creator brief-delivery path; (4) per-creator alert preferences on onboarding_profiles; (5) the auto-alert trigger chain; (6) the Clay adapters and Dashboard surfaces.DISTANCEWeeks. Seven pieces: (1) create the creator_alerts migration, ~half a day; (2) add per-creator alert preferences (columns on onboarding_profiles or a side table), ~half a day; (3) build the creator-facing email path — templates, SMTP invocation, tracking pixels for open detection, click tracking for link detection, ~2-3 days; (4) wire the auto-alert trigger chain from trend.matched events above preference threshold, ~1-2 days; (5) register alert_creator_about_trend Clay adapter + Dashboard manual-alert button, ~1 day; (6) build the Dashboard alert log page, ~1-2 days; (7) add quiet-hours logic and the queue-and-delay handling, ~1 day. Total: 7-11 days. If the full creator portal is scoped in, add several more days for in-app notification delivery; the spec above treats in-app as optional and email as the minimum shippable channel.DEPENDENCIESLayer 2 (Event Log), Layer 4 (Creator Operations Graph), Layer 5 (Capability Surfaces), Layer 6 (Managed Autonomy). TS-1, TS-2, and TS-3 must ship first — TS-4 depends on the match rationale (from TS-2) and optionally the draft brief (from TS-3) to produce a meaningful alert. TS-4 is the only workflow in this document that depends on a user-facing surface (creator portal or creator email delivery) that does not yet exist at all; the shipping path must decide explicitly whether to build the portal alongside or ship email-only first.
Build gaps summary
WorkflowSchemaAPIDashboardClay toolAuto-triggerSTATUSTS-1 Trend Monitoring✅⚠️ (cron exists, not scheduled)⚠️ (page fabricates data)❌❌ (blocked on Phase F)PARTIAL — Math.random in production surfaceTS-2 Trend-to-Creator Matching❌ (table missing)❌❌❌❌NOT BUILTTS-3 Auto-Brief from Trend✅ (most) / ❌ (trend_match_id)⚠️ (autodream exists, not scheduled; no real-time trigger)⚠️ (review queue works, no trend-sourced tagging)⚠️ (ActionButton only)❌PARTIAL — no real-time, no adapter, cron unscheduledTS-4 Creator Alerts❌ (table missing)❌❌❌❌NOT BUILT
Tool Call Registry contribution (Trend Scout)
ToolKindWrite pairPurposeSTATUSget_trendsReadn/aTS-1 — query active trends filtered by nicheNOT BUILTrefresh_trendsWritepropose / realTS-1 — manual out-of-cycle scan, rate-limitedNOT BUILTget_trend_matchesReadn/aTS-2 — query matches for a trend, or matches for a creatorNOT BUILTgenerate_brief_from_trendWritepropose / realTS-3 — manual draft from (trend, creator) pairPARTIAL — ActionButton path exists, no adapteralert_creator_about_trendWritepropose / realTS-4 — manual creator-facing alertNOT BUILT
Phase G exit criteria
Phase G is complete when:

The cultural-scan cron is scheduled in vercel.json and its last-successful-run timestamp is visible on the Trend Radar page.
The Trend Radar page renders from cultural_events with zero Math.random() calls and zero hardcoded FOMO strings — verified by grepping src/app/agency/trends/ for Math.random and for "12 agencies" / "3.2x" to confirm both return zero matches.
trend_creator_matches and creator_alerts tables exist with their migrations merged. The five-tables-that-don't-exist count from audit §1.C.4 is down from 5 to 3.
The four trigger chains are live: scheduled scan → cultural_events, trend.promoted → matching pass, trend.matched above threshold → auto-brief draft, trend.matched above creator preference → creator alert.
All five Trend Scout Clay tools are registered as proper adapters (propose/real pairs for writes, read tools for queries) — no ActionButton-only paths remain for TS workflows.
Each of the four workflows passes a round-trip test: operator initiates in Clay → state reflected in Dashboard, operator initiates in Dashboard → state reflected in Clay, autonomous trigger fires → state reflected in both.
A docs/ai-employees/trend-scout.md file exists summarizing the four workflows, their current STATUS, the separately-tracked SKILL-002 fingerprint-rewrite dependency that bounds TS-2 matching accuracy, and the creator-portal dependency that bounds TS-4's in-app delivery.
The ~10 per-agency per-24-hours auto-brief storm cap is configured, tested with a simulated multi-trend-promotion spike, and surfaces a clear message in Clay when the cap engages.

Phase G ships natively on top of Layer 5 (Capability Surfaces unified in Phase E) — unlike Phases B, C, and D, there is no Phase-E refactor required after Phase G completes.
END OF AI EMPLOYEE 3 — TREND SCOUT / CONTENT STRATEGIST.