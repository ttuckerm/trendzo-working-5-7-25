Docs & Tutorials

APIs
- GET `/api/docs/index` → list docs and tutorial assets

UI steps
- Visit `/operations-center#docs` to view counts and link to `/docs` (if implemented separately)

Runbooks
- Ensure at least one doc exists in `docs_index` and one `tutorial_asset` row.

Troubleshooting
- Seed via POST `/api/qa/seed`



