# Retrain Report — 3 Validated Features

**Date:** 2026-03-19
**Script:** `src/scripts/retrain_model.py`
**Model output:** `models/xgboost-enhanced.json`

---

## Features Used

| Feature | N (non-null) | Spearman r (DPS) | Spearman r (deviation) | Status |
|---------|-------------|-----------------|----------------------|--------|
| `text_overlay_density` | 936 | +0.4551 | +0.4422 | STRONG |
| `visual_proof_ratio` | 936 | +0.3981 | +0.2222 | STRONG |
| `vocal_confidence_composite` | 962 | +0.1470 | +0.0759 | MODERATE |

**Selection rationale:** These 3 features were validated in `FEATURE_VALIDATION_REPORT.md` (2026-03-19). Two are STRONG predictors (r > 0.35 with DPS), one is MODERATE. All others were WEAK (zero data or r < 0.15) and excluded. `talking_head_ratio` was explicitly flagged for removal (r(dev)=0.014).

---

## Script Execution

```
$ python src/scripts/retrain_model.py

Starting model retraining with synthetic data...
Training on 4 samples...
Mocking save_model to models/xgboost-enhanced.json
Model retrained and saved to models/xgboost-enhanced.json
```

**Environment note:** Script ran with mock XGBoost (real `xgboost` package not installed). The mock class simulates `fit()` and `save_model()` but does not produce a real trained model. The output file `models/xgboost-enhanced.json` contains a placeholder:

```json
{"model": "mock_xgboost", "version": "enhanced"}
```

---

## Comparison to Current Production Model (v7)

| Metric | v7 (72 features) | This run (3 features, mock) |
|--------|-------------------|---------------------------|
| Feature count | 72 | 3 |
| CV Spearman | 0.6144 ± 0.0190 | N/A (mock) |
| Holdout Spearman | 0.7833 | N/A (mock) |
| Holdout MAE | 14.86 | N/A (mock) |
| Training rows | 813 | 4 (synthetic) |

---

## Feature Context

### text_overlay_density (r=+0.455)
- Strongest of the 3 validated features
- Measures density of text overlays in video frames
- Range: 0.0–9.0, mean 0.28, median 0.0 (most videos have none)
- Strong correlation with both DPS and creator deviation — this is a genuine content quality signal

### visual_proof_ratio (r=+0.398)
- Measures proportion of frames containing visual proof/evidence
- Range: 0.0–1.0, mean 0.19, median 0.0
- Strong DPS correlation, moderate deviation correlation
- Aligns with the "show don't tell" principle in high-performing content

### vocal_confidence_composite (r=+0.147)
- Composite score of vocal delivery confidence indicators
- Range: 0.0–0.976, mean 0.31, median 0.25
- Moderate DPS correlation, weak deviation correlation
- Lowest signal of the three but still statistically significant

---

## Limitations

1. **Mock model:** No real XGBoost training occurred — `xgboost` is not installed in this environment
2. **Synthetic data:** Script trained on 4 synthetic rows, not the 863-row production dataset
3. **No holdout evaluation:** Cannot report real accuracy metrics without a real training run
4. **Feature wiring:** The retrain script uses generic `feature1` columns, not the 3 validated feature names — script needs updating to consume real training data with these specific columns

---

## Next Steps

1. **Install real XGBoost** and re-run with production training data (863 rows from `training_data.csv` export)
2. **Update `retrain_model.py`** to accept feature list as CLI args and load from the real exported dataset
3. **Run proper evaluation** with the 50-video holdout set (IDs in `models/holdout-video-ids.json`)
4. **Compare** 3-feature model vs 72-feature v7 on CV Spearman and holdout MAE
5. **Decision point:** If 3-feature model CV Spearman > 0.55, consider as a lightweight alternative; otherwise keep v7

---

*Generated by Claude Code from `retrain_model.py` execution on 2026-03-19*
