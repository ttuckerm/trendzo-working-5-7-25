# Done Checklist — Dashboard

- [ ] `chart-discovery` renders from GET `/api/discovery/rollups`
- [ ] `chart-decay` renders from GET `/api/discovery/rollups`
- [ ] Empty state shows hint when arrays empty

## Manual Test Script
1) Open Dashboard tab.
2) Verify both charts render bars.
3) If empty, check Ops → Recompute; re-check.
