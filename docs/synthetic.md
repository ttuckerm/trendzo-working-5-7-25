### Synthetic Monitoring

- Headless probe hits critical routes every N minutes; writes to `synthetic_probe`.
- UI shows last 10 probe results and alerts on >2 failures.
- Preflight runs a local probe once.


