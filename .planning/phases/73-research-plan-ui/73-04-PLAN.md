---
phase: 73-research-plan-ui
plan: 04
type: execute
wave: 2
depends_on: ["73-01"]
files_modified:
  - src/components/workflow/ExemplarSwoop.tsx
  - src/components/workflow/ExemplarCard.tsx
  - src/lib/hooks/useExemplars.ts
autonomous: true
---

<objective>
Build Exemplar Swoop component for research phase - find and track viral accounts.

Purpose: Enable users to research successful creators and track them for inspiration.
Output: Exemplar search, add, and library management integrated into Research phase.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-data-model/71-CONTEXT.md
@.planning/phases/72-api-routes/72-03-SUMMARY.md
@.planning/phases/73-research-plan-ui/73-01-SUMMARY.md

UI reference from designs:
- "Exemplar Swoop" section with platform tabs
- Search input for hashtags/keywords/creators
- Pro Tip about finding outlier accounts
- Linked exemplars displayed as cards

Key decision from context:
"Exemplar Swoop: Both - save exemplars to a global library AND link to current workflow"
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useExemplars hook</name>
  <files>src/lib/hooks/useExemplars.ts</files>
  <action>
Create React hook for exemplar management:

```typescript
export function useExemplars(workflowId?: string) {
  const [library, setLibrary] = useState<ExemplarLibraryItem[]>([]);
  const [workflowExemplars, setWorkflowExemplars] = useState<ExemplarLibraryItem[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  // Library actions (global)
  const fetchLibrary = async (platform?: Platform, tags?: string[]) => { ... }
  const addToLibrary = async (exemplar: AddExemplarInput) => { ... }
  const removeFromLibrary = async (id: string) => { ... }

  // Workflow actions (per-workflow)
  const fetchWorkflowExemplars = async () => { ... }
  const linkToWorkflow = async (exemplarId: string) => { ... }
  const addAndLinkToWorkflow = async (exemplar: AddExemplarInput) => { ... }
  const unlinkFromWorkflow = async (exemplarId: string) => { ... }

  // Effects
  useEffect(() => {
    fetchLibrary();
    if (workflowId) fetchWorkflowExemplars();
  }, [workflowId]);

  return { library, workflowExemplars, loading, error, ...actions };
}
```

Use fetch() to call Phase 72 API routes:
- GET/POST/DELETE /api/workflows/exemplars (global library)
- GET/POST/DELETE /api/workflows/:id/exemplars (workflow linking)

Support the "add_and_link" mode for adding new exemplar AND linking to workflow.
  </action>
  <verify>TypeScript compiles without errors</verify>
  <done>Hook provides full exemplar CRUD with library and workflow linking</done>
</task>

<task type="auto">
  <name>Task 2: Create ExemplarSwoop component</name>
  <files>src/components/workflow/ExemplarSwoop.tsx, src/components/workflow/ExemplarCard.tsx</files>
  <action>
**ExemplarSwoop.tsx:**
Main component matching UI design:

Header: "🌊 Exemplar Swoop"
Description: "Find 25 accounts in your niche, track their viral videos, and reverse engineer what works"

Platform tabs:
- TikTok (active, with icon)
- YouTube (with "Soon" badge, disabled)
- Instagram (with "Soon" badge, disabled)

Search input:
- Placeholder: "Search hashtags, keywords, or creators..."
- Search button or enter to search

Pro Tip box:
"Find people with disproportional astronomical views compared to their peers. If someone has 200K views when others have 2K, they're doing something right. Study their hooks, their consistency, and their monetization."

Linked exemplars section:
- Grid of ExemplarCard components
- "Add manually" button for entering exemplar without search
- Empty state: "No exemplars linked yet. Search above or add manually."

Add Exemplar Modal (triggered by search result click or "Add manually"):
- Platform (pre-selected)
- Account handle
- Account name
- Video URL (optional)
- Video title (optional)
- Notes (optional)
- Tags (comma-separated)
- "Add to Library & Link" button

**ExemplarCard.tsx:**
Card component for displaying linked exemplar:
- Platform badge (TikTok/YouTube/Instagram icon)
- Account handle (@username)
- Account name
- Video title (if present)
- Notes excerpt (if present)
- "Unlink" button (removes from workflow, keeps in library)
- Click to view/edit details

Grid layout: 4 columns on desktop, 2 on tablet, 1 on mobile.
  </action>
  <verify>Components render without errors</verify>
  <done>ExemplarSwoop shows platform tabs, search, and linked exemplars grid</done>
</task>

<task type="auto">
  <name>Task 3: Integrate ExemplarSwoop into ResearchPhase</name>
  <files>src/components/workflow/phases/ResearchPhase.tsx</files>
  <action>
Add ExemplarSwoop component to ResearchPhase after the Goals section:

```tsx
// In ResearchPhase.tsx, add after GoalsKPIs section:
<section className="space-y-4">
  <ExemplarSwoop
    workflowId={workflow.id}
    onExemplarLinked={() => { /* optional callback */ }}
  />
</section>
```

Position it before the "Save Research & Continue" footer button.

The ExemplarSwoop component is self-contained and manages its own state via useExemplars hook.
  </action>
  <verify>Research phase shows ExemplarSwoop section</verify>
  <done>Exemplar Swoop integrated into Research phase workflow</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes for new files
- [ ] useExemplars hook fetches and manages exemplar data
- [ ] ExemplarSwoop renders with platform tabs and search
- [ ] Can add exemplar and link to workflow
- [ ] ExemplarCard displays linked exemplars
- [ ] Unlink removes from workflow but keeps in library
</verification>

<success_criteria>
- All 3 tasks completed
- Exemplar Swoop matches UI design
- "Both" pattern works (add to library AND link to workflow)
- Linked exemplars persist with workflow
</success_criteria>

<output>
After completion, create `.planning/phases/73-research-plan-ui/73-04-SUMMARY.md`
</output>
