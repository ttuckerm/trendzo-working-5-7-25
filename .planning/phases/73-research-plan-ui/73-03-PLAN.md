---
phase: 73-research-plan-ui
plan: 03
type: execute
wave: 2
depends_on: ["73-01"]
files_modified:
  - src/components/workflow/phases/PlanPhase.tsx
  - src/components/workflow/GoldenPillars.tsx
  - src/components/workflow/TikTokSEO.tsx
  - src/components/workflow/FourByFourMethod.tsx
autonomous: false
---

<objective>
Build complete Plan phase UI with Golden Pillars, TikTok SEO, and 4×4 Method.

Purpose: Second workflow phase where user structures their content strategy.
Output: Fully functional Plan phase with all form fields saving to workflow step data.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-data-model/71-CONTEXT.md
@.planning/phases/73-research-plan-ui/73-01-SUMMARY.md

UI reference from designs:
- Golden Pillars: Education, Entertainment, Inspiration, Validation (single-select cards)
- TikTok SEO: Core Anchor Keyword + Related Search Terms
- 4×4 Method: Hook, Proof, Value, CTA text areas
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Plan phase container</name>
  <files>src/components/workflow/phases/PlanPhase.tsx</files>
  <action>
Create the Plan phase main component:

```typescript
interface PlanPhaseProps {
  workflow: WorkflowRunWithDetails;
  onSave: (data: PlanPhaseData) => Promise<void>;
  onBack: () => void;
  onContinue: () => void;
}

interface PlanPhaseData {
  pillar: 'education' | 'entertainment' | 'inspiration' | 'validation';
  seo: {
    core_keyword: string;
    related_terms: string;
  };
  four_by_four: {
    hook: string;      // First 4 seconds
    proof: string;     // Next 4 seconds
    value: string;     // The meat
    cta: string;       // Call to action
  };
}
```

Layout (matching UI design):
- "Content Planning" header with description
- "Golden Pillars of Content" section with GoldenPillars
- "TikTok SEO Strategy" section with TikTokSEO
- "The 4×4 Method" section with FourByFourMethod
- Pro Tip box at bottom

Auto-save on field changes (debounced 500ms) via PUT /api/workflows/:id/steps/2
Load initial values from workflow.steps[1].input_data

Footer: "← Back to Research" button + "Continue to Creation →" button
  </action>
  <verify>Component renders without errors</verify>
  <done>Plan phase shows all sections, saves data on change</done>
</task>

<task type="auto">
  <name>Task 2: Create Plan form components</name>
  <files>src/components/workflow/GoldenPillars.tsx, src/components/workflow/TikTokSEO.tsx, src/components/workflow/FourByFourMethod.tsx</files>
  <action>
**GoldenPillars.tsx:**
4 selectable cards matching UI design:

1. Education (📚 emoji)
   - "How-tos, tutorials, tips, resources"
   - "Builds TRUST" badge

2. Entertainment (🎭 emoji)
   - "Trends, current events, humor, pranks"
   - "Most SHARED" badge

3. Inspiration (✨ emoji)
   - "Transformations, lifestyle, before/after"
   - "ASPIRATIONAL" badge

4. Validation (💬 emoji)
   - "Your story, opinions, polarizing content"
   - "Drives ENGAGEMENT" badge

Single-select cards. Selected card has red/pink highlight border.
Grid layout: 4 columns on desktop, 2 on mobile.

**TikTokSEO.tsx:**
Two inputs with helper text matching UI design:

1. "Core Anchor Keyword" text input
   - Placeholder: "e.g., 'best content strategy'"
   - Helper: "This should appear in your first sentence, on-screen text, and description"

2. "Related Search Terms" text input
   - Placeholder: "e.g., 'how to get more views'"
   - Helper: "Use TikTok's search suggestions to find related terms"

Blue TikTok icon badge in header.

**FourByFourMethod.tsx:**
4 text areas in a 2x2 grid matching UI design:

1. HOOK (First 4 seconds) - Red numbered badge "1"
   - Description: "Attention grabbing, pain points, polarizing, curiosity-inducing"
   - Placeholder: "What will you say to grab attention in the first 4 seconds?"

2. PROOF (Next 4 seconds) - Red numbered badge "2"
   - Description: "Stats, studies, testimonials, social proof to lower their guard"
   - Placeholder: "What proof will you show to make them comfortable staying?"

3. VALUE (The meat) - Red numbered badge "3"
   - Description: "How-tos, step-by-step, lists, the recipe - why they're here"
   - Placeholder: "What's the actual value/content you're delivering?"

4. CALL TO ACTION - Red numbered badge "4"
   - Description: "Based on your content purpose: Know → Follow | Like → Share | Trust → Link in bio"
   - Placeholder: "What do you want them to do after watching?"

Pro Tip box at bottom: "Use list format (5 things, 3 tips) - they work like recipe videos..."
  </action>
  <verify>Each component renders correctly in isolation</verify>
  <done>All 3 form components styled and functional with onChange callbacks</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Plan phase UI with Golden Pillars, TikTok SEO, and 4×4 Method</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Navigate to an existing workflow (or create new and complete Research)
    3. Advance to Plan phase (should be at phase 2)
    4. Verify Plan phase displays with all sections:
       - Golden Pillars: 4 cards selectable (single-select)
       - TikTok SEO: keyword and related terms inputs
       - 4×4 Method: 4 text areas in grid
    5. Fill in all fields, wait 1 second, refresh page
    6. Verify data persisted (fields should show your input)
    7. Click "Back to Research" - verify you return to phase 1
    8. Return to Plan, click "Continue to Creation"
    9. Verify you advance to Phase 3 (should show placeholder)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes for new files
- [ ] Plan phase renders all form sections
- [ ] Data saves to workflow step via API
- [ ] Back button navigates to Research phase
- [ ] Continue button advances to Create phase
</verification>

<success_criteria>
- All 3 tasks completed
- Plan phase matches UI design aesthetic
- Data persists across page refreshes
- 4×4 Method data ready for Create phase pre-fill
- User approved visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/73-research-plan-ui/73-03-SUMMARY.md`
</output>
