---
phase: 73-research-plan-ui
plan: 02
type: execute
wave: 2
depends_on: ["73-01"]
files_modified:
  - src/components/workflow/phases/ResearchPhase.tsx
  - src/components/workflow/NicheSelector.tsx
  - src/components/workflow/DemographicsChips.tsx
  - src/components/workflow/ContentPurposeSelector.tsx
  - src/components/workflow/GoalsKPIs.tsx
autonomous: false
---

<objective>
Build complete Research phase UI with niche selection, demographics, content purpose, and goals.

Purpose: First workflow phase where user defines their content strategy foundation.
Output: Fully functional Research phase with all form fields saving to workflow step data.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-data-model/71-CONTEXT.md
@.planning/phases/73-research-plan-ui/73-01-SUMMARY.md

UI reference from designs:
- Niche dropdown (category selector)
- Demographics chips (18-24, 25-34, 35-44, 45+)
- Content Purpose cards (Know/Like/Trust)
- Goals & KPIs (goal dropdown + target views input)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Research phase container</name>
  <files>src/components/workflow/phases/ResearchPhase.tsx</files>
  <action>
Create the Research phase main component:

```typescript
interface ResearchPhaseProps {
  workflow: WorkflowRunWithDetails;
  onSave: (data: ResearchPhaseData) => Promise<void>;
  onContinue: () => void;
}

interface ResearchPhaseData {
  niche: string;
  demographics: string[];  // ["18-24", "25-34"]
  content_purpose: 'know' | 'like' | 'trust';
  goal: string;
  target_views: number;
}
```

Layout (matching UI design):
- "Research Phase" header with description
- "Define Your Niche" section with NicheSelector
- "Target Audience Demographics" section with DemographicsChips
- "Content Purpose" section with ContentPurposeSelector
- "Set Goals & KPIs" section with GoalsKPIs

Auto-save on field changes (debounced 500ms) via PUT /api/workflows/:id/steps/1
Load initial values from workflow.steps[0].input_data

Footer button: "Save Research & Continue to Planning →"
  </action>
  <verify>Component renders without errors</verify>
  <done>Research phase shows all sections, saves data on change</done>
</task>

<task type="auto">
  <name>Task 2: Create form components</name>
  <files>src/components/workflow/NicheSelector.tsx, src/components/workflow/DemographicsChips.tsx, src/components/workflow/ContentPurposeSelector.tsx, src/components/workflow/GoalsKPIs.tsx</files>
  <action>
**NicheSelector.tsx:**
Dropdown with existing niches from the codebase (check for niche list in types or constants).
If no list exists, use common niches: ["Fitness", "Finance", "Fashion", "Food", "Gaming", "Tech", "Lifestyle", "Education", "Entertainment", "Business"]
Style: Dark dropdown matching admin UI (bg-gray-700, border-gray-600)

**DemographicsChips.tsx:**
Multi-select chips for age demographics (from UI design):
- 18-24 years
- 25-34 years
- 35-44 years
- 45+ years

Chips are toggle-able (outline when unselected, filled when selected).
Allow multiple selections.
Style: Rounded pills with border, highlight on selection.

**ContentPurposeSelector.tsx:**
3 cards matching UI design:
1. KNOW (🖐️ emoji) - "Get them to know you" - CTA: "Follow for more"
2. LIKE (❤️ emoji) - "Build rapport & trust" - CTA: "Like & Share"
3. TRUST (🤝 emoji) - "Convert to customers" - CTA: "Link in bio"

Single-select cards. Selected card has highlighted border/background.
Each card shows: emoji, title, description, CTA example.

**GoalsKPIs.tsx:**
Two inputs:
1. "Primary Content Goal" - dropdown: ["Brand Awareness", "Lead Generation", "Sales", "Community Building", "Education"]
2. "Target Views" - number input with default 10,000

Style: Side-by-side inputs (grid-cols-2 on desktop)
  </action>
  <verify>Each component renders correctly in isolation</verify>
  <done>All 4 form components styled and functional with onChange callbacks</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Research phase UI with niche selection, demographics, content purpose, and goals</what-built>
  <how-to-verify>
    1. Run: npm run dev
    2. Create a new workflow at /workflows (click "New Workflow")
    3. Verify Research phase displays with all sections:
       - Niche dropdown works
       - Demographics chips toggle on/off
       - Content Purpose cards are selectable (only one at a time)
       - Goals dropdown and target views input work
    4. Fill in all fields, wait 1 second (debounce), refresh page
    5. Verify data persisted (fields should show your selections)
    6. Click "Save Research & Continue to Planning"
    7. Verify you advance to Phase 2 (Plan)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes for new files
- [ ] Research phase renders all form sections
- [ ] Data saves to workflow step via API
- [ ] Continue button advances to Plan phase
</verification>

<success_criteria>
- All 3 tasks completed
- Research phase matches UI design aesthetic
- Data persists across page refreshes
- User approved visual verification
</success_criteria>

<output>
After completion, create `.planning/phases/73-research-plan-ui/73-02-SUMMARY.md`
</output>
