---
phase: 73-research-plan-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/workflows/page.tsx
  - src/app/workflows/[id]/page.tsx
  - src/components/workflow/WorkflowStepper.tsx
  - src/components/workflow/WorkflowLayout.tsx
  - src/lib/hooks/useWorkflow.ts
autonomous: true
---

<objective>
Create workflow dashboard and shell layout with phase navigation.

Purpose: Establish the workflow UI foundation that both Research and Plan phases will use.
Output: Workflow list page, workflow detail page with stepper, shared layout components.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/71-data-model/71-CONTEXT.md
@.planning/phases/72-api-routes/72-01-SUMMARY.md

Source files:
@src/lib/types/workflow.ts
@src/app/admin/upload-test/page.tsx (reference for styling patterns)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create useWorkflow hook</name>
  <files>src/lib/hooks/useWorkflow.ts</files>
  <action>
Create React hook for workflow state management:

```typescript
export function useWorkflow(workflowId?: string) {
  // State
  const [workflow, setWorkflow] = useState<WorkflowRunWithDetails | null>(null);
  const [workflows, setWorkflows] = useState<WorkflowSummary[]>([]);
  const [loading, setLoading] = useState(false);
  const [error, setError] = useState<string | null>(null);

  // Actions
  const fetchWorkflows = async (status?: WorkflowStatus) => { ... }
  const fetchWorkflow = async (id: string) => { ... }
  const createWorkflow = async (metadata?: WorkflowMetadata) => { ... }
  const advancePhase = async () => { ... }
  const navigateToPhase = async (phase: number) => { ... }
  const updateStep = async (phase: number, data: Partial<StepInputData>) => { ... }
  const abandonWorkflow = async () => { ... }

  // Effects
  useEffect(() => {
    if (workflowId) fetchWorkflow(workflowId);
  }, [workflowId]);

  return { workflow, workflows, loading, error, ...actions };
}
```

Use fetch() to call the API routes from Phase 72:
- GET/POST /api/workflows
- GET/PATCH/DELETE /api/workflows/:id
- POST /api/workflows/:id/advance
- PUT /api/workflows/:id/steps/:phase

Handle { data, error } response pattern. Convert API errors to user-friendly messages.
  </action>
  <verify>TypeScript compiles without errors</verify>
  <done>Hook provides full workflow CRUD operations with loading/error states</done>
</task>

<task type="auto">
  <name>Task 2: Create workflow layout components</name>
  <files>src/components/workflow/WorkflowLayout.tsx, src/components/workflow/WorkflowStepper.tsx</files>
  <action>
**WorkflowStepper.tsx:**
6-phase horizontal stepper matching the UI designs:
- Phases: Research → Plan → Create → Optimize → Publish → Engage & Learn
- Current phase highlighted (filled background)
- Completed phases show checkmark
- Future phases grayed out
- Clicking completed phases triggers back-navigation (via navigateToPhase)
- Cannot click future phases (enforced visually)

Use Tailwind CSS matching existing admin dark theme (bg-gray-800, text-white, etc.)

Icons per phase (use emoji or simple SVG):
- Research: 🔍
- Plan: 📋
- Create: 🎬
- Optimize: ⚡
- Publish: 📤
- Engage: 📊

**WorkflowLayout.tsx:**
Wrapper component with:
- Header: "Viral Content Creator" title + Current DPS Score (0.0 until content)
- WorkflowStepper
- Children slot for phase content
- Footer: Back button (if not phase 1) + Continue button

Props: workflow, onBack, onContinue, children
  </action>
  <verify>Components render without errors in isolation</verify>
  <done>Stepper shows 6 phases with click navigation, Layout wraps phase content</done>
</task>

<task type="auto">
  <name>Task 3: Create workflow list and detail pages</name>
  <files>src/app/workflows/page.tsx, src/app/workflows/[id]/page.tsx</files>
  <action>
**Workflow List Page (src/app/workflows/page.tsx):**
- "Your Workflows" header
- "New Workflow" button (creates workflow, redirects to /workflows/:id)
- List of workflows with:
  - Status badge (active/completed/abandoned)
  - Current phase indicator
  - Started date
  - Click to open
- Show "No workflows yet" empty state
- Filter tabs: All | Active | Completed

**Workflow Detail Page (src/app/workflows/[id]/page.tsx):**
- Use WorkflowLayout wrapper
- Load workflow via useWorkflow(id)
- Render phase-specific content based on workflow.current_phase:
  - Phase 1: <ResearchPhase /> (stub for now)
  - Phase 2: <PlanPhase /> (stub for now)
  - Phase 3-6: "Coming soon" placeholders
- Handle loading state (skeleton)
- Handle error state (workflow not found)
- Wire up back/continue buttons to advancePhase/navigateToPhase

Create stub components for Research and Plan that just show "Phase N content goes here".
  </action>
  <verify>npm run build passes, pages render at /workflows and /workflows/:id</verify>
  <done>Workflow dashboard lists workflows, detail page shows stepper and phase content</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes for new files
- [ ] Workflow list page loads at /workflows
- [ ] Creating new workflow redirects to detail page
- [ ] Stepper shows 6 phases with correct highlighting
- [ ] Back-navigation works from stepper clicks
</verification>

<success_criteria>
- All 3 tasks completed
- Workflow hook manages state correctly
- Layout components match UI design aesthetic
- Pages integrate with Phase 72 API routes
</success_criteria>

<output>
After completion, create `.planning/phases/73-research-plan-ui/73-01-SUMMARY.md`
</output>
