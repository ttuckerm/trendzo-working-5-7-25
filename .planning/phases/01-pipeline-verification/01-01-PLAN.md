---
phase: 01-pipeline-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
---

<objective>
Verify the Pack 1/2/3/V system executes correctly and prediction runs complete successfully.

Purpose: Confirm production readiness before deploying Pack system changes.
Output: Verification report confirming all packs work end-to-end.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/lib/prediction/runPredictionPipeline.ts
@src/lib/rubric-engine/index.ts
@src/lib/prediction/__tests__/pack-gating.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all Pack-related tests</name>
  <files>N/A (verification only)</files>
  <action>
Run the pack, pipeline, and rubric test suites to verify all 26+ tests pass:
```bash
npm test -- --testPathPattern="(pack|pipeline|rubric)"
```

Capture the test output showing:
- Total tests passed/failed
- Any error messages if failures occur
  </action>
  <verify>Command output shows "Tests: X passed, 0 failed"</verify>
  <done>All pack-related tests pass with 0 failures</done>
</task>

<task type="auto">
  <name>Task 2: Verify TypeScript compilation</name>
  <files>N/A (verification only)</files>
  <action>
Run TypeScript type check to ensure no type errors in pack system:
```bash
npx tsc --noEmit
```

Focus on any errors in:
- src/lib/rubric-engine/
- src/lib/prediction/runPredictionPipeline.ts
- src/app/admin/upload-test/
  </action>
  <verify>Command completes with no errors</verify>
  <done>TypeScript compilation passes with 0 errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Pack 1/2/3/V integration in /admin/upload-test page</what-built>
  <how-to-verify>
    1. Run: `npm run dev`
    2. Visit: http://localhost:3000/admin/upload-test
    3. Enter transcript: "Want to know how to make $500 a day from your couch? Most people think you need special skills but that's completely wrong. Let me show you three simple steps anyone can do starting today."
    4. Select niche: side_hustles
    5. Click Submit/Predict
    6. Verify Pack 1 panel shows:
       - 9 attribute scores (1-10 each)
       - 7 idea lego checkmarks
       - Hook analysis with type and clarity score
    7. Verify Pack 2 panel shows:
       - Before/After DPS estimates
       - Up to 3 improvement suggestions
    8. Verify Pack 3 panel shows:
       - Viral mechanics with strength indicators
       - Summary text
       - Confidence score
    9. Check browser console for errors (should be none)
  </how-to-verify>
  <resume-signal>Type "approved" if all packs display correctly, or describe any issues</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify prediction_runs completion status</name>
  <files>N/A (verification only)</files>
  <action>
After the smoke test, verify the prediction run completed properly in the database.

Use Supabase SQL Editor or CLI to run:
```sql
SELECT
  id AS run_id,
  status,
  predicted_dps_7d,
  CASE WHEN raw_result IS NOT NULL THEN 'PRESENT' ELSE 'NULL' END AS raw_result,
  completed_at,
  created_at
FROM prediction_runs
ORDER BY created_at DESC
LIMIT 5;
```

Verify:
- Most recent run has status='completed' or status='success' (NOT 'running')
- raw_result column shows 'PRESENT' (not NULL)
- completed_at is populated
  </action>
  <verify>Query shows status='completed' or 'success' and raw_result='PRESENT'</verify>
  <done>Prediction runs finalize correctly with all fields populated</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All 26+ pack-related tests pass
- [ ] TypeScript compiles without errors
- [ ] Smoke test on /admin/upload-test shows all 4 packs
- [ ] prediction_runs row has status='completed' and raw_result populated
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors in Pack 1/2/3/V display
- Database shows proper run completion
</success_criteria>

<output>
After completion, create `.planning/phases/01-pipeline-verification/01-01-SUMMARY.md`
</output>
