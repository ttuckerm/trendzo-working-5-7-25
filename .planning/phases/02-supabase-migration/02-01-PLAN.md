---
phase: 02-supabase-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
user_setup:
  - service: supabase
    why: "Migration must be applied via Supabase SQL Editor (production database)"
    dashboard_config:
      - task: "Run migration SQL"
        location: "Supabase Dashboard → SQL Editor"
        details: "Paste contents of 20260115_transcription_status_tracking.sql and execute"
---

<objective>
Apply transcription status tracking migration to production Supabase database.

Purpose: Enable tracking of transcription source, confidence, and pack metadata for each prediction run.
Output: New columns and view available in production database.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@supabase/migrations/20260115_transcription_status_tracking.sql
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <action>Run migration SQL in Supabase SQL Editor</action>
  <instructions>
    The migration file is ready at: `supabase/migrations/20260115_transcription_status_tracking.sql`

    Steps:
    1. Open Supabase Dashboard: https://app.supabase.com
    2. Select your project (Trendzo)
    3. Go to SQL Editor (left sidebar)
    4. Create a new query
    5. Copy and paste the ENTIRE contents of `20260115_transcription_status_tracking.sql`
    6. Click "Run" to execute
    7. Verify no errors appear in the output
  </instructions>
  <verification>Query executes without errors, shows "Success"</verification>
  <resume-signal>Type "done" when migration is complete, or describe any errors</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verify migration applied correctly</name>
  <files>N/A (verification only)</files>
  <action>
Run verification script to confirm new columns exist and are queryable:
```bash
node scripts/verify-run-finalization.mjs
```

This should show the new columns are available. Additionally, check one of the transcription columns directly.
  </action>
  <verify>Script runs without errors, columns are visible in output</verify>
  <done>New transcription columns confirmed in prediction_runs table</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Transcription status tracking columns in production database</what-built>
  <how-to-verify>
    1. Run: `npm run dev`
    2. Visit: http://localhost:3000/admin/upload-test
    3. Enter a transcript and run a prediction
    4. Go to Supabase Dashboard → Table Editor → prediction_runs
    5. Find the most recent row
    6. Verify new columns exist:
       - transcription_source (should show value like "user_provided")
       - transcription_confidence (should show number 0.0-1.0)
       - resolved_transcript_length (should show character count)
       - pack1_meta (should show JSONB with source/provider)
       - pack2_meta (should show JSONB with source/provider)
  </how-to-verify>
  <resume-signal>Type "approved" if columns are populated, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Migration SQL executed without errors
- [ ] New columns visible in prediction_runs table
- [ ] v_recent_prediction_runs view created
- [ ] New prediction run populates transcription columns
</verification>

<success_criteria>

- All tasks completed
- Migration applied to production
- Columns populated on new prediction runs
- No errors in pipeline or API
</success_criteria>

<output>
After completion, create `.planning/phases/02-supabase-migration/02-01-SUMMARY.md`
</output>
