---
phase: 74-create-optimize
plan: 03
type: execute
wave: 2
depends_on: ["74-01", "74-02"]
files_modified:
  - src/components/workflow/ScriptEditor.tsx
  - src/components/workflow/phases/CreatePhase.tsx
  - src/components/workflow/index.ts
autonomous: true
---

<objective>
Create ScriptEditor with side-by-side Plan reference and editable script, then integrate all Create phase components.

Purpose: Enable side-by-side script writing with Plan phase 4x4 Method as reference.
Output: ScriptEditor.tsx with two-column layout and full CreatePhase integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-create-optimize/74-CONTEXT.md

# Use patterns from:
@.planning/phases/74-create-optimize/74-01-SUMMARY.md
@.planning/phases/74-create-optimize/74-02-SUMMARY.md
@src/components/workflow/FourByFourMethod.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ScriptEditor component</name>
  <files>src/components/workflow/ScriptEditor.tsx</files>
  <action>
Create side-by-side script editor with Plan reference:

**Props interface:**
```typescript
interface ScriptEditorProps {
  // Plan phase reference (read-only)
  planReference: {
    hook: string;
    proof: string;
    value: string;
    cta: string;
  };
  // Editable script
  script: {
    hook: string;
    proof: string;
    value: string;
    cta: string;
  };
  // Change handlers
  onScriptChange: (field: 'hook' | 'proof' | 'value' | 'cta', value: string) => void;
}
```

**UI Structure - Two Column Layout:**

1. **Left Column (40% width)** - "Your Plan" reference:
   - Header: "Your 4x4 Plan" with icon
   - 4 read-only cards showing Plan phase content:
     - Hook (with pink/red badge)
     - Proof
     - Value
     - CTA
   - Styling: Dimmed background (bg-gray-900/30), text-gray-400
   - Each section shows the text from planReference

2. **Right Column (60% width)** - "Your Script" editor:
   - Header: "Your Script" with pencil icon
   - 4 editable text areas matching left column structure:
     - Same section labels (Hook, Proof, Value, CTA)
     - Placeholder: "Write your actual script for the [section]..."
     - Word count display under each textarea
   - Styling: Active background (bg-gray-800/50), cyan focus rings

3. **Visual connection:**
   - Subtle horizontal lines or arrows connecting matching sections
   - Or: matching colored left borders on both columns' cards

**Copy button** - Small button on each Plan section: "Copy to script"
- Copies that section's plan text to the corresponding script field

Style with dark theme, cyan (#06b6d4) accent for Create phase.
  </action>
  <verify>npx tsc --noEmit src/components/workflow/ScriptEditor.tsx</verify>
  <done>ScriptEditor shows two-column layout with plan reference and editable script</done>
</task>

<task type="auto">
  <name>Task 2: Integrate VideoInput and ScriptEditor into CreatePhase</name>
  <files>src/components/workflow/phases/CreatePhase.tsx</files>
  <action>
Update CreatePhase to include all components:

1. **Import components:**
   ```typescript
   import { VideoInput } from '../VideoInput';
   import { ScriptEditor } from '../ScriptEditor';
   import { ContentMetadata } from '../ContentMetadata';
   ```

2. **Update component layout to show all sections:**

   Section 1: Video Input
   - Full width VideoInput component
   - Pass workflowId, videoUrl from state, onVideoChange handler
   - Video upload updates the state and triggers auto-save

   Section 2: Script Editor (side-by-side)
   - ScriptEditor component
   - planReference: loaded from Plan phase step (phase 2) input_data.four_by_four
   - script: from CreatePhase state (pre-filled from plan on initial load)
   - onScriptChange: updates state and triggers debounced save

   Section 3: Content Metadata
   - ContentMetadata component
   - All fields from state
   - onChange handlers update state and trigger debounced save

3. **Data flow:**
   - On mount: Load Plan phase 4x4 as both planReference AND initial script values
   - On change: Update local state immediately, debounce save to API
   - Save format: PUT /api/workflows/:id/steps/3 with full CreatePhaseData

4. **Supabase Storage upload (on video change):**
   - When VideoInput provides a blob, upload to Supabase Storage
   - Bucket: 'workflow-videos'
   - Path: `{workflowId}/video_{timestamp}.webm`
   - Store the public URL in state as video_url
   - Use @supabase/supabase-js createClient from existing setup

Note: If Supabase Storage is not yet configured, add a TODO comment and skip upload - just store blob URL locally for now.
  </action>
  <verify>npx tsc --noEmit src/components/workflow/phases/CreatePhase.tsx</verify>
  <done>CreatePhase shows VideoInput, ScriptEditor (side-by-side), and ContentMetadata</done>
</task>

<task type="auto">
  <name>Task 3: Update exports and verify integration</name>
  <files>src/components/workflow/index.ts</files>
  <action>
1. Add ScriptEditor to barrel exports:
   ```typescript
   export { ScriptEditor } from './ScriptEditor';
   ```

2. Verify all Phase 74 components are exported:
   - CreatePhase
   - ContentMetadata
   - VideoInput
   - ScriptEditor

3. Run full TypeScript check on workflow components directory.
  </action>
  <verify>npx tsc --noEmit src/components/workflow/*.tsx src/components/workflow/phases/*.tsx</verify>
  <done>All Create phase components exported and type-check clean</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx tsc --noEmit` passes for all files
- [ ] ScriptEditor shows two-column layout
- [ ] Plan reference is read-only, script is editable
- [ ] Copy buttons work to transfer plan text to script
- [ ] CreatePhase integrates VideoInput, ScriptEditor, ContentMetadata
- [ ] Auto-save works for all fields
- [ ] Plan phase 4x4 pre-fills script on load
</verification>

<success_criteria>
- All tasks completed
- No TypeScript errors
- ScriptEditor side-by-side layout functional
- Full CreatePhase integration working
- Auto-save triggers on all field changes
</success_criteria>

<output>
After completion, create `.planning/phases/74-create-optimize/74-03-SUMMARY.md`
</output>
