import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Marketing Studio | Trendzo Admin',
  description: 'Super Admin Marketing Studio - Create viral campaigns with AI-powered tools',
};

export default function MarketingStudioLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div className="flex flex-col h-full">
      <div className="flex-1 overflow-auto">
        {children}
      </div>
    </div>
  );
}