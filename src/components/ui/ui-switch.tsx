"use client";

/**
 * Switch Component Wrapper
 * 
 * This is a compatibility layer that ensures Switch imports work consistently
 * It re-exports the Switch component from the base switch.tsx file
 */

import { Switch } from './switch';

export { Switch };
export default Switch; 