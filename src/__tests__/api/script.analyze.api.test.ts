import { extractScriptFeatures } from '@/lib/script/features'

describe('API /api/script/analyze', () => {
  it('returns shape with probScript and recommendations', async () => {
    ;(global as any).fetch = jest.fn(async (url: string, opts: any) => ({ ok: true, json: async () => ({}) }))
    const features = extractScriptFeatures('POV: you are doing 3 mistakes. Follow for more!')
    expect(features.numbers).toBeGreaterThanOrEqual(0)
  })
})








































































































































