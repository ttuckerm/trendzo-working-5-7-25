// import { Firestore } from 'firebase/firestore';
// import { Auth } from 'firebase/auth';
// import { FirebaseStorage } from 'firebase/storage';
// import { Analytics } from 'firebase/analytics';

// /**
//  * Firebase app configuration
//  */
// export interface FirebaseConfig {
//   apiKey: string;
//   authDomain: string;
//   projectId: string;
//   storageBucket: string;
//   messagingSenderId: string;
//   appId: string;
//   measurementId?: string;
// }

// /**
//  * Firebase app instances
//  */
// export interface FirebaseInstances {
//   db: Firestore;
//   auth: Auth;
//   storage: FirebaseStorage;
//   analytics?: Analytics;
// }

// This file primarily defined types for the Firebase initialization (src/lib/firebase/firebase.ts)
// which has been neutralized. These types are likely no longer needed in their original context.
// If other parts of the application still somehow use these exact type names for other purposes,
// they would need to be redefined or sourced differently.

export {}; // Add an empty export to ensure it's treated as a module if other files still import it. 